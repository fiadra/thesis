\chapter{Quantum graphs over \( 3 \times 3 \) matrices}
\label{ch:quantum-graphs-m3}

The classification of quantum graphs on \( 2 \times 2 \) matrices was
carried out independently by \textcite{matsuda22} and \textcite{gromada22}.
In this chapter, we attempt a partial classification of quantum graphs on
\( 3 \times 3 \) matrices. This case turns out to be considerably harder
than the \( 2 \times 2 \) case, and a classification of comparable
simplicity is likely out of reach altogether, as we show in
Lemma~\ref{lemma:hard-action}. We conclude the chapter by comparing our
results with those of \textcite{hayes26}.

\section{Pauli matrices and their generalisations}
\label{sec:pauli-matrices-generalisations}

\subsection{Pauli matrices}
\label{subsec:pauli-matrices-m3}

An important basis of the Lie algebra \( \mathfrak{sl}(2) \)
is given by the Pauli matrices
\[
    \sigma_1 =
    \begin{pmatrix} 0 & 1 \\ 1 & 0
    \end{pmatrix}, \qquad
    \sigma_2 =
    \begin{pmatrix} 0 & -i \\ i & 0
    \end{pmatrix}, \qquad
    \sigma_3 =
    \begin{pmatrix} 1 & 0 \\ 0 & -1
    \end{pmatrix}.
\]
The Pauli matrices are Hermitian, traceless, and linearly independent; \\
since \( \dim_{\mathbb{C}} \mathfrak{sl}(2) = 3 \) by
\ref{prop:su2-dimension}, they form a
basis of \( \mathfrak{sl}(2) \). They were already encountered
in Example~\ref{example:pauli-matrices}.

Because the Pauli matrices are Hermitian rather than skew-Hermitian, they
do not themselves belong to \( \mathfrak{su}(2) \). Instead, it is the
matrices
\[
    i\sigma_1, \qquad i\sigma_2, \qquad i\sigma_3
\]
that form a basis of \( \mathfrak{su}(2) \), being both traceless and
skew-Hermitian.

\subsection{Generalised Pauli matrices}
\label{subsec:generalised-pauli-matrices}

In dimensions greater than two, the role of the Pauli matrices is played
by the \emph{generalised Pauli matrices}, also known as the \emph{clock}
and \emph{shift} matrices. In dimension three, they are defined by
\[
    P =
    \begin{pmatrix} 1 & 0 & 0 \\ 0 & \omega & 0 \\ 0 & 0 & \omega^2
    \end{pmatrix}, \qquad
    Q =
    \begin{pmatrix} 0 & 0 & 1 \\ 1 & 0 & 0 \\ 0 & 1 & 0
    \end{pmatrix},
\]
where \( \omega = \exp(2\pi i/3) \).

The matrices \( P \) and \( Q \) are unitary and satisfy
\[
    P^3 = Q^3 = I,
\]
together with the commutation relation
\[
    PQ = \omega\, QP.
\]

The clock and shift matrices are related by the discrete Fourier
transform. The Fourier matrix is
\[
    F = \frac{1}{\sqrt{3}}
    \begin{pmatrix} 1 & 1 & 1 \\ 1 & \omega & \omega^2 \\ 1 & \omega^2 & \omega
    \end{pmatrix},
\]
which is unitary, i.e.\ satisfies \( F^*F = FF^* = I \), and exchanges
the roles of the clock and shift operators according to
\[
    FPF^* = Q^{-1}, \qquad FQF^* = P.\label{dft-relations}
\]
Consequently, \( P \) and \( Q \) are unitarily equivalent and represent
two complementary bases of the three-dimensional Hilbert space. This
duality is the finite-dimensional analogue of the relation between
position and momentum operators, and is a fundamental feature of the
generalised Pauli matrices.

To simplify later calculations, we first establish the following
auxiliary lemma.

\begin{lemma}
    \label{lemma:qp-commutation}
    For all \( a, b \in \mathbb{Z} \), the generalised Pauli
    matrices \( P, Q \) satisfy
    \[
        Q^a P^b = \omega^{-ab}\, P^b Q^a.
    \]
\end{lemma}

\begin{proof}
    We first establish the base case
    \( a=b=1 \), namely \( QP = \omega^{-1}PQ \), and then extend
    to general \( a, b \) by induction.

    For the base case, apply both sides to a basis vector \( e_i \).
    Since \( Pe_i = \omega^i e_i \),
    \[
        (QP)e_i = Q(\omega^i e_i) = \omega^i\, e_{i+1},
    \]
    while, since \( Qe_i = e_{i+1} \) and \( Pe_{i+1} = \omega^{i+1}e_{i+1} \),
    \[
        (PQ)e_i = P e_{i+1} = \omega^{i+1}\, e_{i+1}.
    \]
    Comparing the two, \( (QP)e_i = \omega^{-1}(PQ)e_i \) for every
    \( i \), so \( QP = \omega^{-1} PQ \), which is exactly
    \( Q^1P^1 = \omega^{-1\cdot 1} P^1Q^1 \).

    We now show \( Q^aP = \omega^{-a}PQ^a \) for every
    \( a \in \mathbb{Z} \), by induction on \( a \). The case \( a=0 \)
    is trivial, and \( a=1 \) is the base case above. For the forward
    inductive step, suppose \( Q^aP = \omega^{-a}PQ^a \) holds; then
    \[
        Q^{a+1}P = Q(Q^aP) = Q(\omega^{-a}PQ^a) = \omega^{-a}(QP)Q^a
        = \omega^{-a}(\omega^{-1}PQ)Q^a = \omega^{-(a+1)}PQ^{a+1},
    \]
    using the base case \( QP=\omega^{-1}PQ \) in the second-to-last
    step. For the backward inductive step, we multiply the base equation
    \( QP = \omega^{-1}PQ \) on the left and right by \( Q^{-1} \) to obtain
    \( P Q^{-1} = \omega^{-1} Q^{-1} P \), which rearranges to
    \( Q^{-1}P = \omega PQ^{-1} \). Suppose \( Q^aP = \omega^{-a}PQ^a
    \) holds; then
    \[
        Q^{a-1}P = Q^{-1}(Q^aP) = Q^{-1}(\omega^{-a}PQ^a) =
        \omega^{-a}(Q^{-1}P)Q^a
        = \omega^{-a}(\omega PQ^{-1})Q^a = \omega^{-(a-1)}PQ^{a-1}.
    \]
    This proves \( Q^aP = \omega^{-a}PQ^a \) for all \( a \in \mathbb{Z} \).

    Finally, we extend to general \( b \) by an analogous induction
    on \( b \), using the relation \( Q^aP = \omega^{-a}PQ^a \) just
    established as the new base case. The case \( b=0 \) is
    trivial. For the forward inductive step, suppose
    \( Q^aP^b = \omega^{-ab}P^b Q^a \) holds; then
    \[
        Q^aP^{b+1} = (Q^aP^b)P = \omega^{-ab}P^b(Q^aP)
        = \omega^{-ab}P^b(\omega^{-a}PQ^a)
        = \omega^{-a(b+1)}P^{b+1}Q^a,
    \]
    completing the forward induction. For the backward inductive step,
    we multiply the relation \( Q^aP = \omega^{-a}PQ^a \) on the left and right
    by \( P^{-1} \) to obtain \( P^{-1}Q^a = \omega^{-a}Q^aP^{-1} \), meaning
    \( Q^aP^{-1} = \omega^a P^{-1}Q^a \). Suppose \( Q^aP^b =
    \omega^{-ab}P^b Q^a \) holds; then
    \[
        Q^aP^{b-1} = (Q^aP^b)P^{-1} = \omega^{-ab}P^b(Q^aP^{-1})
        = \omega^{-ab}P^b(\omega^a P^{-1}Q^a)
        = \omega^{-a(b-1)}P^{b-1}Q^a,
    \]
    completing the induction. Hence \( Q^aP^b = \omega^{-ab}P^b Q^a
    \) for all \( a,b \in \mathbb{Z} \).
\end{proof}

\begin{lemma}
    \label{lemma:pq-orthogonal}
    The matrices \( P^aQ^b \), \( a,b \in \{0,1,2\} \), form an
    orthogonal basis of \( M_{3}(\mathbb{C}) \) with respect to the
    Hilbert--Schmidt inner product \( \langle A,B \rangle =
    \operatorname{Tr}(A^*B) \).
\end{lemma}

\begin{proof}
    Since \( \dim_{\mathbb{C}} M_{3}(\mathbb{C}) = 9 \) and there are
    exactly \( 3^2 = 9 \) matrices of the form \( P^aQ^b \) for \(
    a,b \in \{0,1,2\} \), it suffices to show that they are pairwise orthogonal.

    Recall that \( P \) and \( Q \) are unitary matrices, so \( P^* =
    P^{-1} \) and \( Q^* = Q^{-1} \). This gives \( (P^aQ^b)^* =
    (Q^b)^*(P^a)^* = Q^{-b}P^{-a} \). The Hilbert--Schmidt inner
    product expands as
    \[
        \langle P^aQ^b, P^cQ^d \rangle
        = \operatorname{Tr}\big((P^aQ^b)^*P^cQ^d\big)
        = \operatorname{Tr}\big(Q^{-b}P^{-a}P^cQ^d\big)
        = \operatorname{Tr}\big(Q^{-b}P^{c-a}Q^d\big).
    \]
    Because Lemma~\ref{lemma:qp-commutation} holds for all integer
    exponents, we can directly apply it to the terms \( Q^{-b} \) and
    \( P^{c-a} \):
    \[
        Q^{-b}P^{c-a} = \omega^{-(-b)(c-a)}\, P^{c-a}Q^{-b} =
        \omega^{b(c-a)}\, P^{c-a}Q^{-b}.
    \]
    Substituting this back into the trace yields
    \[
        \langle P^aQ^b, P^cQ^d \rangle
        = \operatorname{Tr}\big(\omega^{b(c-a)} P^{c-a}Q^{-b}Q^d\big)
        = \omega^{b(c-a)} \operatorname{Tr}\big(P^{c-a}Q^{d-b}\big).
    \]

    To evaluate this trace, we consider two cases. First, if \( b
    \neq d \), the exponent \( d-b \in \{-2,-1,1,2\} \), meaning \(
    Q^{d-b} \) is a nontrivial cyclic permutation matrix. Multiplying
    the diagonal matrix \( P^{c-a} \) by \( Q^{d-b} \) results in a
    matrix with zeros everywhere on the main diagonal, forcing \(
    \operatorname{Tr}(P^{c-a}Q^{d-b}) = 0 \).

    Second, if \( b = d \), then \( Q^{d-b} = Q^0 = I \), and the
    inner product simplifies to
    \[
        \langle P^aQ^b, P^cQ^b \rangle = \operatorname{Tr}(P^{c-a}).
    \]
    Since \( P^{c-a} = \operatorname{diag}(1, \omega^{c-a},
    \omega^{2(c-a)}) \), its trace is given by a geometric series:
    \[
        \operatorname{Tr}(P^{c-a}) = 1 + \omega^{c-a} + \omega^{2(c-a)}
        =
        \begin{cases}
            3, & a = c, \\
            0, & a \neq c.
        \end{cases}
    \]
    (The latter case follows from the identity \( 1 + \omega +
        \omega^2 = 0 \), which applies since \( a \neq c \) implies \(
    c-a \not\equiv 0 \pmod 3 \)).

    Combining both cases, we conclude that
    \[
        \langle P^aQ^b, P^cQ^d \rangle = 3\,\delta_{ac}\delta_{bd}.
    \]
    Therefore, the nine matrices \( P^aQ^b \) are pairwise orthogonal
    and form an orthogonal basis for \( M_{3}(\mathbb{C}) \).
\end{proof}

Excluding the identity matrix from this basis leaves eight traceless
matrices, which together span \( \mathfrak{sl}(3) \). A basis
of \( \mathfrak{su}(3) \) is then obtained by passing to suitable
skew-Hermitian linear combinations of these eight matrices, exactly as
the passage from \( \sigma_i \) to \( i\sigma_i \) produced a basis of
\( \mathfrak{su}(2) \) above. In this sense, the clock and shift matrices
furnish a natural higher-dimensional generalisation of the Pauli
matrices, suited to describing three-level quantum systems.

\section{Classification of self-adjoint subspaces spanned by a
    subset of the generalised Pauli basis \( \left\{ P^{a} Q^{b}
\right\}_{a,b \in \{ 0,1,2 \}} \)}
\label{sec:classification-self-adjoint-subspaces}

By the results of the previous section, it suffices to focus on
self-adjoint subspaces of \( \mathfrak{sl}(3) \). We stress at the
outset, however, that the classification carried out in this section
concerns only \emph{self-adjoint subspaces spanned by a subset of
the generalised Pauli basis} \( \{P^aQ^b\}_{a,b \in \{0,1,2\}} \).
Equivalently, by
Lemma~\ref{lemma:pq-orthogonal}, subspaces that are a direct sum of
some of the adjoint-invariant blocks introduced below (and not
an arbitrary self-adjoint subspace of \( \mathfrak{sl}(3) \)). Since
\( \mathfrak{sl}(3) \) is eight-dimensional, an arbitrary subspace need
not be spanned by any subset of a fixed basis: for instance,
\( \operatorname{span}\{P+Q\} \) is a perfectly legitimate
one-dimensional self-adjoint subspace of \( \mathfrak{sl}(3) \) that is
not of this restricted form, and there are in fact continuous families
of subspaces of each dimension that are not coordinate subspaces with
respect to \( \{P^aQ^b\}_{a,b \in \{0,1,2\}} \). The classification
below should therefore
be read as covering a specific, tractable subfamily of the loop-free,
undirected quantum graphs on \( M_{3}(\mathbb{C}) \), rather than all of them.

\subsection{Fundamental adjoint-invariant pairs}
\label{subsec:fundamental-adjoint-invariant-pairs}

Since \( P \) and \( Q \) are unitary, we have \( P^* = P^{-1} \) and
\( Q^* = Q^{-1} \), meaning the conjugate transpose of an arbitrary
basis matrix is \( (P^aQ^b)^* = Q^{-b}P^{-a} \). By
Lemma~\ref{lemma:qp-commutation}, we can commute these matrices to obtain
\[
    Q^{-b}P^{-a} = \omega^{-(-b)(-a)} P^{-a}Q^{-b} = \omega^{-ab} P^{-a}Q^{-b}.
\]
Because \( P^3 = Q^3 = I \), we can rewrite the negative exponents
modulo 3, yielding the relation
\[
    (P^aQ^b)^* = \omega^{-ab}\, P^{3-a}Q^{3-b}.
\]
For any subspace spanned by a subset of \( \{P^aQ^b\} \) to be
self-adjoint, it must be closed under conjugate transposition.
Consequently, whenever \( P^aQ^b \) is included in the generating
set, its conjugate counterpart \( P^{3-a}Q^{3-b} \) must also be
included. This pairing naturally partitions the eight non-identity
basis matrices into four disjoint, adjoint-invariant subspaces of dimension two:

\begin{align*}
    \mathcal{P}_1 &= \{P, P^2\} && \text{(purely diagonal block)} \\
    \mathcal{P}_2 &= \{Q, Q^2\} && \text{(purely shift block)} \\
    \mathcal{P}_3 &= \{PQ, P^2Q^2\} && \text{(first mixed block)} \\
    \mathcal{P}_4 &= \{P^2Q, PQ^2\} && \text{(second mixed block)}
\end{align*}

Any choice of \( S \in P(\left\{ 1, 2, 3, 4 \right\}) \) is valid
and gives rise to a self-adjoint subspace, where \( P(A) \) is the
power set of \( A \).
For instance, the choice \( S = \{1,3\} \) gives the four-dimensional
subspace
\[
    W_{1,3}= \operatorname{span}(\mathcal{P}_1 \cup \mathcal{P}_3 ) =
    \operatorname{span}\{P, P^2, PQ, P^2Q^2\}.
\]

\subsection{Action of the discrete Fourier transform}
\label{subsec:dft-action-m3}

Let \( F \) denote the \( 3 \times 3 \) discrete Fourier transform
matrix. Recall that, by~\ref{dft-relations},
\[
    FPF^* = Q^2, \qquad FQF^* = P.
\]

\begin{lemma}
    \label{lemma:dft-block-swap}
    On the fundamental blocks \( \mathcal{P}_k \), conjugation by
    \( F \) induces a pair-swapping involution:
    \[
        F\mathcal{P}_1F^* = \mathcal{P}_2, \qquad
        F\mathcal{P}_2F^* = \mathcal{P}_1, \qquad
        F\mathcal{P}_3F^* = \mathcal{P}_4, \qquad
        F\mathcal{P}_4F^* = \mathcal{P}_3.
    \]
\end{lemma}

\begin{proof}
    Since \( F \) is unitary and satisfies \( FPF^* = Q^2 \) and \(
    FQF^* = P \),
    conjugation by \( F \) is a \( * \)-automorphism sending
    \( P \mapsto Q^2 \) and \( Q \mapsto P \). Hence, for any
    integers \( a,b \),
    \[
        F(P^aQ^b)F^* = (FPF^*)^a(FQF^*)^b = Q^{2a}P^b.
    \]
    Applying this to the basis matrices of \( \mathcal{P}_1 \), we have
    \[
        FPF^* = Q^2, \qquad FP^2F^* = Q^4 = Q,
    \]
    so \( F\mathcal{P}_1F^* = \operatorname{span}\{Q^2,Q\} = \mathcal{P}_2 \).
    Because conjugation by \( F^2 \) maps \( P \mapsto Q^2 \mapsto
    P^2 \) and \( P^2 \mapsto Q \mapsto P \), it preserves the
    subspace \( \mathcal{P}_1 \). Therefore, applying \( F(\cdot)F^*
    \) to both sides of \( F\mathcal{P}_1F^* = \mathcal{P}_2 \)
    yields \( F\mathcal{P}_2F^* = \mathcal{P}_1 \).

    Similarly, applying the \( F \)-conjugation to the generators of
    \( \mathcal{P}_3 \) yields
    \[
        F(PQ)F^* = Q^2P, \qquad F(P^2Q^2)F^* = Q^4P^2 = QP^2.
    \]
    Using the commutation relation
    \( Q^aP^b = \omega^{-ab}P^b Q^a \) from
    Lemma~\ref{lemma:qp-commutation}, we can rewrite
    \( Q^2P = \omega^{-2}PQ^2 = \omega PQ^2 \) and
    \( QP^2 = \omega^{-2}P^2Q = \omega P^2Q \). Thus,
    \[
        F(PQ)F^* = \omega\, PQ^2, \qquad F(P^2Q^2)F^* = \omega\, P^2Q.
    \]
    Since scalar multiples do not affect the linear span,
    \[
        F\mathcal{P}_3F^*
        = \operatorname{span}\{\omega PQ^2, \omega P^2Q\}
        = \operatorname{span}\{PQ^2,P^2Q\} = \mathcal{P}_4.
    \]
    By the same logic as before, applying \( F(\cdot)F^* \) once more gives
    \( F\mathcal{P}_4F^* = \mathcal{P}_3 \).
\end{proof}

\subsection{Subspace classification}
\label{subsec:subspace-classification}

Because the fundamental building blocks \( \mathcal{P}_k \) are
non-overlapping \( 2 \)-element sets, every self-adjoint subspace
spanned by a subset of \( \{P^aQ^b\}_{a,b \in \{0,1,2\}} \) is formed
by taking a direct sum of a subset of these blocks (including the
empty set, which yields the zero subspace).

\begin{proposition}
    \label{prop:subspace-classification-by-dimension}
    Every self-adjoint subspace \( V \subseteq M_{3}(\mathbb{C})
    \) generated by
    \( P \) and \( Q \) is classified up to unitary conjugacy by its
    dimension \( m = \dim(V) \),
    as recorded in Table~\ref{tab:classification}.
\end{proposition}

% TODO: give ref
The proof will be postponed until later.

\begin{figure}[h]
    \begin{center}
        \begin{tabular}{| c | c | c |}
            \hline
            \( m \) & subspaces & count \\
            \hline
            \( 1,3,5,7 \) & none exist & \( 0 \) \\
            \hline
            \( 0 \) & \( V_0 = \{0\} \) & \( 1 \) \\
            \hline
            \( 2 \) & \( V_k = \operatorname{span}(\mathcal{P}_k) \),
            \( k \in \{1,2,3,4\} \) & \( 4 \) \\
            \hline
            \( 4 \) & \( W_{i,j} = \operatorname{span}(\mathcal{P}_i
            \cup \mathcal{P}_j) \), \( 1 \le i < j \le 4 \) & \( 6 \) \\
            \hline
            \( 6 \) & \( U_k = \operatorname{span}\big(\bigcup_{i
            \neq k} \mathcal{P}_i\big) \), \( k \in \{1,2,3,4\} \) & \( 4 \) \\
            \hline
            \( 8 \) & \( \mathfrak{sl}(3) =
                \operatorname{span}\big(\bigcup_{k=1}^4
            \mathcal{P}_k\big) \) & \( 1 \) \\
            \hline
        \end{tabular}
        \caption{Classification of self-adjoint subspaces
            \( V \subseteq M_{3}(\mathbb{C}) \) generated by \( P \)
            and \( Q \) by
        dimension.}
        \label{tab:classification}
    \end{center}
\end{figure}

\begin{corollary}
    \label{cor:sixteen-subspaces}
    Including the trivial subspace \( V_0 \), there exist precisely
    \( 16 \) self-adjoint subspaces generated by \( P \) and \( Q \).
\end{corollary}

This classification reduces to elementary combinatorics once the
blocks \( \mathcal{P}_1, \dots, \mathcal{P}_4 \) are fixed. Since these
four blocks partition the eight non-identity Pauli generators into
disjoint reflection-invariant pairs, choosing a self-adjoint
subspace \( V \) of this restricted form amounts to nothing more than
choosing a subset
\( S \subseteq \{1,2,3,4\} \) of blocks to include, with
\[
    V = \operatorname{span}\left(\bigcup_{k \in S} \mathcal{P}_{k}\right).
\]
There are \( 2^4 = 16 \) such subsets in total, matching the count in
the corollary. Moreover, since each included block contributes exactly
two dimensions, the resulting dimension \( \dim(V) \) is always even,
explaining why \( V \) can never have odd dimension. The counts in
each row of the table are then simply the binomial coefficients
\( \binom{4}{r} \), for \( r = |S| \) blocks chosen, which is also why the
table is symmetric under \( m \leftrightarrow 8-m \).

\subsection{Full unitary equivalence within each dimension}
\label{subsec:full-unitary-equivalence}

The action of the discrete Fourier transform \( F \) considered above
only produces \( 4 \) orbits among the six four-dimensional subspaces
\( W_{i,j} \), namely \( \{W_{1,2}\}, \{W_{3,4}\},\) \(\{W_{1,3},W_{2,4}\},
\{W_{1,4},W_{2,3}\} \). We now show that this is an artifact of
restricting attention to \( F \) alone: once a second natural unitary
is brought in, \emph{all six} subspaces of dimension \( 4 \) turn out
to be unitarily equivalent. More generally, all
subspaces of any fixed dimension \( m \in \{0,2,4,6,8\} \), among those
generated by \( P \) and \( Q \), are unitarily equivalent.

Consider the diagonal phase matrix
\[
    S =
    \begin{pmatrix}
        1 & 0 & 0 \\
        0 & 1 & 0 \\
        0 & 0 & \omega \\
    \end{pmatrix}.
\]
Since \( S \) is diagonal, it commutes with \( P \), so
\( SPS^{-1} = P \) and hence \( S \) fixes the block
\( \mathcal{P}_1 = \{P,P^2\} \). Its effect on \( Q \) is
more interesting.

\begin{lemma}\label{lem:S-action}
    Conjugation by \( S \) cyclically permutes the blocks
    \( \mathcal{P}_2, \mathcal{P}_3, \mathcal{P}_4 \), while fixing
    \( \mathcal{P}_1 \). Explicitly,
    \[
        \begin{aligned}
            SQS^{-1} &= \omega^{-1} PQ, & \qquad SQ^2S^{-1} &= P^2Q^2, \\
            S(PQ)S^{-1} &= \omega^{-1}P^2Q, & \qquad S(P^2Q^2)S^{-1} &= PQ^2.
        \end{aligned}
    \]
\end{lemma}

\begin{proof}
    Write \( e_1,e_2,e_3 \) for the standard basis, so that
    \( Qe_i = e_{i+1} \) (indices mod \( 3 \)) and \( S e_i = s_i e_i \)
    with \( s_1 = s_2 = 1 \), \( s_3 = \omega \). Then
    \[
        (SQS^{-1})e_i = s_i^{-1}(SQ)e_i = s_i^{-1}s_{i+1}\,e_{i+1},
    \]
    and computing the ratios \( s_{i+1}/s_i \) for \( i=1,2,3 \) gives
    \( 1, \omega, \omega^2 \) respectively. Comparing with
    \( (PQ)e_i = p_{i+1}e_{i+1} \), where \( p_1=1,p_2=\omega,p_3=\omega^2 \),
    which gives coefficients \( \omega,\omega^2,1 \), we see that
    \( SQS^{-1} \) has exactly the coefficients of \( PQ \) shifted by
    a global factor of \( \omega^{-1} \); that is,
    \( SQS^{-1} = \omega^{-1}PQ \).

    Squaring the second relation, and using the commutation relation
    \( QP = \omega^{-1}PQ \) to simplify
    \( (PQ)^2 = P(QP)Q = \omega^{-1}P^2Q^2 \), we obtain
    \[
        SQ^2S^{-1} = (SQS^{-1})^2 = \omega^{-2}(PQ)^2
        = \omega^{-2}\cdot\omega^{-1}P^2Q^2 = P^2Q^2,
    \]
    since \( \omega^{-3}=1 \).

    For the remaining two identities, use that \( S \) commutes with
    \( P \):
    \[
        S(PQ)S^{-1} = P\,(SQS^{-1}) = P\cdot\omega^{-1}PQ = \omega^{-1}P^2Q,
    \]
    \[
        S(P^2Q^2)S^{-1} = P^2\,(SQ^2S^{-1}) = P^2\cdot P^2Q^2 = P^4Q^2 = PQ^2,
    \]
    using \( P^3 = I \) in the last step. Collecting these four
    identities: \( S \) sends \( \{Q,Q^2\} = \mathcal{P}_2 \) onto
    \( \{PQ, P^2Q^2\} = \mathcal{P}_3 \) (up to phase), and sends
    \( \mathcal{P}_3 \) onto \( \{P^2Q,PQ^2\} = \mathcal{P}_4 \) (up to
    phase). By the same computation applied once more, or simply by
    counting (three blocks permuted among themselves by a single
        conjugation must be permuted cyclically or fixed, and we
    have already ruled out the latter), \( S \) also sends
    \( \mathcal{P}_4 \) onto \( \mathcal{P}_2 \). This proves the claim.
\end{proof}

In terms of the block indices \( \{1,2,3,4\} \), conjugation by
\( F \) induces the permutation \( \sigma_F = (1\,2)(3\,4) \) (as noted
above), while conjugation by \( S \) induces the \( 3 \)-cycle
\( \sigma_S = (2\,3\,4) \).

\begin{remark}
    \label{rem:a4-no-subgroup-order-six}
    A basic fact from finite group theory will be used below: the
    alternating group \( A_4 \) (of order \( 12 \)) has \emph{no}
    subgroup of order \( 6 \). Indeed, a subgroup of order \( 6 \)
    would have index \( 2 \) in \( A_4 \) and hence be normal, but the
    only normal subgroups of \( A_4 \) are the trivial subgroup, the
    Klein four-group \( V_4 = \{e,(12)(34),(13)(24),(14)(23)\} \), and
    \( A_4 \) itself, none of order \( 6 \).
\end{remark}

\begin{proposition}\label{prop:full-equivalence}
    The permutations \( \sigma_F \) and \( \sigma_S \) generate the
    full alternating group \( A_4 \) acting on the block indices
    \( \{1,2,3,4\} \). In particular this action is transitive on the
    six unordered pairs \( \{i,j\} \subseteq \{1,2,3,4\} \), i.e. for
    any unordered pairs \( \left\{ i, j \right\} \), \( \left\{ i' ,
    j' \right\} \)
    there is a permutation \( \tau \in A_4 \) such that \(
    \tau(\left\{ i, j \right\}) = \left\{ i' , j' \right\} \).
\end{proposition}

\begin{proof}
    Both \( \sigma_F \) (a double transposition) and \( \sigma_S \) (a
    \( 3 \)-cycle) are even permutations, so the group \( H \) they
    generate is a subgroup of \( A_4 \). Because \( H \) contains an
    element of order \( 2 \) and an element of order \( 3 \), Lagrange's
    Theorem dictates that the order of \( H \) must be divisible by both
    \( 2 \) and \( 3 \). Since \( 2 \) and \( 3 \) are coprime, their least
    common multiple, \( 6 \), must also divide \( |H| \). But \( A_4
    \) has no subgroup of order \( 6 \) by
    Remark~\ref{rem:a4-no-subgroup-order-six}, and the only divisor
    of \( |A_4|=12 \) that is a multiple of \( 6 \) and admits a
    subgroup is \( 12 \) itself; hence \( H = A_4 \).

    It remains to verify that \( A_4 \) acts transitively on the set
    of six unordered pairs \( P = \{ \{i,j\} \subseteq
    \{1,2,3,4\} : i \neq j \} \).
    We first establish that \( A_4 \) is \( 2 \)-transitive on \(
    \{1,2,3,4\} \), meaning it acts transitively on the set \(
    O = \{ (i,j) \in \{1,2,3,4\}^2 : i \neq j \} \) of all
    \( 4 \times 3 = 12 \) ordered distinct pairs. Consider the action
    of \( A_4 \) on \( O \) given by \( \tau \cdot (i,j) =
    (\tau(i), \tau(j)) \). The point-stabilizer of the ordered pair
    \( (1,2) \) is:
    \[
        \operatorname{Stab}_{A_4}((1,2)) = \{ \tau \in A_4 : \tau(1)
        = 1 \text{ and } \tau(2) = 2 \}.
    \]
    Every non-identity element in \( A_4 \) is either a \( 3 \)-cycle
    (which fixes exactly one element) or a product of two disjoint \(
    2 \)-cycles (which fixes zero elements). Consequently, no
    non-identity element of \( A_4 \) can fix two distinct points
    simultaneously, forcing \( \operatorname{Stab}_{A_4}((1,2)) =
    \{e_{A_4}\} \).

    By the Orbit-Stabilizer Theorem, the size of the orbit containing
    \( (1,2) \) is:
    \[
        |\operatorname{Orb}_{A_4}((1,2))| =
        \frac{|A_4|}{|\operatorname{Stab}_{A_4}((1,2))|} = \frac{12}{1} = 12.
    \]
    Since the orbit size matches the total number of ordered pairs \(
    |O| = 12 \), \( A_4 \) acts transitively on ordered pairs.

    Finally, for any two unordered pairs \( \{i,j\}, \{i',j'\} \in
    P \), choosing ordering representatives \( (i,j) \) and
    \( (i',j') \) in \( O \) yields a permutation \( \tau
    \in A_4 \) such that \( \tau(i) = i' \) and \( \tau(j) = j' \). Thus:
    \[
        \tau(\{i,j\}) = \{ \tau(i), \tau(j) \} = \{ i', j' \},
    \]
    which proves that \( A_4 \) is transitive on unordered pairs.
\end{proof}

\begin{proof}[Proof of
    Proposition~\ref{prop:subspace-classification-by-dimension}]
    By Proposition~\ref{prop:full-equivalence}, for any two pairs
    \( \{i,j\}, \{i',j'\} \subseteq \{1,2,3,4\} \) there is a
    permutation \( \tau \in A_4 \) with \( \tau(\{i,j\}) = \{i',j'\} \),
    realized as a word in \( \sigma_F, \sigma_S \) and their inverses.
    Replacing each \( \sigma_F \) or \( \sigma_S \) in this word by the
    corresponding unitary \( F \) or \( S \) yields a unitary \( U \)
    with \( U\mathcal{P}_iU^* = \mathcal{P}_{\tau(i)} \) for every block
    index \( i \) (up to scalar phases on individual generators, which
    do not affect the spans), and hence
    \[
        UW_{i,j}U^* = U\big(\operatorname{span}(\mathcal{P}_i \cup
        \mathcal{P}_j)\big)U^*
        = \operatorname{span}(\mathcal{P}_{\tau(i)} \cup
        \mathcal{P}_{\tau(j)}) = W_{i',j'}.
    \]
    The same argument, using only the transitivity of \( A_4 \) on
    single points, gives the case \( m=2 \); the case \( m=6 \) follows
    by taking orthogonal complements, since \( U_k \) is precisely the
    orthogonal complement of \( V_k \) inside \( \mathfrak{sl}(3) \), and
    unitary conjugation commutes with taking orthogonal complements.
\end{proof}

\section{Loop-free and undirected graphs on \( M_{3}(\mathbb{C}) \) from the
generalised Pauli basis}
\label{sec:classification-loopfree-undirected-m3}

In this section we exhibit, for each possible number of quantum edges
\( m \in \left\{ 0, 2, 4, 6, 8 \right\} \), a representative quantum
graph among those classified in
Section~\ref{sec:classification-self-adjoint-subspaces}, i.e.\
among the loop-free, undirected quantum graphs on \( M_{3}(\mathbb{C}) \) whose
Gromada subspace is a direct sum of the blocks
\( \mathcal{P}_1,\ldots,\mathcal{P}_4 \), realized as a derived
quantum graph of a voltage quantum graph. As already emphasized in
Section~\ref{sec:classification-self-adjoint-subspaces}, this
covers only part of the loop-free, undirected quantum graphs on
\( M_{3}(\mathbb{C}) \); it is \emph{not} a classification of all such graphs.
Thanks to a result by \textcite{tobolski26}, namely
Theorem~\ref{thm:quantum-iso-different-actions}, those graphs are
quantum isomorphic to quantum
graphs on \( (\mathbb{C}^n \rtimes_{\hat\alpha} \hat{\mathbb{Z}}_n,
\psi_\rtimes) \), which, in turn, is isomorphic to \( (M_{3}(\mathbb{C}), 3
\operatorname{Tr}) \) by Lemma~\ref{lem:iso}, and so can be thought of as graphs
on \( (M_{3}(\mathbb{C}), 3\operatorname{Tr}) \). By
\ref{thm:quantum-iso-different-actions}, all those graphs are quantum
isomorphic to classical graphs on 9 vertices.

\begin{theorem}
    \label{thm:4matrices}
    Among the non-empty, loop-free, undirected quantum graphs on
    \( (M_{3}(\mathbb{C}), 3\operatorname{Tr}) \) whose Gromada
    subspace is spanned by a subset of the generalised Pauli basis
    \( \{P^aQ^b\}_{a,b\in \left\{ 0,1,2 \right\}} \), there is
    exactly one isomorphism class in each nontrivial dimension \( m
    \in \{2,4,6,8\} \). In each class, there is a unique quantum
    graph that can be expressed as a derived quantum graph of a
    voltage quantum graph that can be identified with a classical
    voltage graph via \ref{prop:derived-graph-identification}.

    The adjacency matrices for those four derived quantum graphs,
    expressed in the basis of generalised Pauli matrices using the
    formula from Theorem~\ref{thm:gromada}, are
    presented in \ref{fig:four-matrices-m3}.
    \input{matrices}
\end{theorem}

The trivial case \( m=0 \), corresponding to the empty graph, is
omitted, since it carries no meaningful structure to discuss.

The matrices in Proposition~\ref{thm:4matrices} were obtained
by direct computation. For each subspace generated by \( P \) and \( Q \)
as described in Table~\ref{tab:classification}, we calculated the
appropriate matrix using the formula given in
Theorem~\ref{thm:gromada} and compared them with matrices describing
derived quantum graphs coming from quantum voltage graphs \(\widetilde{A}
= \{A_k\}_{k \in \hat{\mathbb{Z}}_3} \) given by the formula
\[
    \widetilde{A} \rtimes_{\hat\alpha} \hat{\mathbb{Z}}_3
    = \sum_{k \in \hat{\mathbb{Z}}_3} m\big( X_k \otimes E_\rtimes^*
    \widetilde{A}_k E_\rtimes \big)\, m^*.
\]
The following figures depict the voltage graphs and derived graphs
that are quantum isomorphic to the representative graphs given as
matrices in
Figure~\ref{fig:four-matrices-m3}. Concretely, the voltage graph with
only self-loops (labeled \( 1,2 \) at every vertex) corresponds to the
case \( m=2 \); adding a single \( 1 \)-voltage triangle to these
loops gives \( m=4 \); adding both a \( 1 \)- and a \( 2 \)-voltage
triangle gives \( m=6 \); and the full voltage graph, with edges of
every voltage \( 0,1,2 \) between every pair of vertices, gives
\( m=8 \), the complete quantum graph.

\input{graphs/ourgraphs}

Since each of these voltage quantum graphs arises, via
Theorem~\ref{thm:quantum-iso-different-actions}, as a
derived quantum graph of an classical voltage graph, the
discussion above in fact exhibits an explicit quantum isomorphism
between each of the four representative quantum graphs of
Theorem~\ref{thm:4matrices} and a classical derived graph on the
nine vertices \( \{u,v,w\} \times \mathbb{Z}_3 \) shown on the
right of each figure.

\begin{proof}[Proof of Theorem~\ref{thm:4matrices}]
    By \ref{prop:subspace-classification-by-dimension}, every \( m
    \)-dimensional Gromada subspace is classified up to unitary
    conjugacy by its dimension. Consequently, \ref{thm:gromada}
    guarantees that the quantum graphs corresponding to these Gromada
    subspaces are isomorphic, which establishes the first statement.

    Next, consider the graphs corresponding to the matrices in
    \ref{fig:four-matrices-m3}. Each of these can be obtained via the
    quantum voltage graph construction detailed in
    \ref{def:quantum-voltage-graph}. Specifically, we take the
    quantum set \( (\mathbb{C}^3, \psi_3) \) equipped with the action
    of the dual group \( \widehat{\mathbb{Z}}_3 = \{ \chi_0, \chi_1,
    \chi_2 \} \) that cyclically rotates the coordinates. Explicitly,
    this action on a vector \( z = (z_0, z_1, z_2) \in \mathbb{C}^3
    \) is given by
    \[
        \chi_k \cdot (z_0, z_1, z_2) = (z_{0+k}, z_{1+k}, z_{2+k}),
    \]
    where the addition is taken \( \pmod 3 \), so that the generator
    \( \chi_1 \) yields \( \chi_1 \cdot (z_0,
    z_1, z_2) = (z_1, z_2, z_0) \).

    By combining this quantum set and group action with the
    appropriate classical voltage graphs, as illustrated in
    \ref{fig:voltage-graph-m2-edges},
    \ref{fig:voltage-graph-m4-edges},
    \ref{fig:voltage-graph-m6-edges}, and
    \ref{fig:voltage-graph-m8-edges}, we obtain the corresponding
    derived quantum graphs. By \ref{lem:iso}, each of these derived
    quantum graphs can be naturally treated as a graph on the quantum
    set \( (M_3 (\mathbb{C}), 3 \operatorname{Tr}) \). Finally,
    \ref{thm:quantum-iso-different-actions} ensures that the
    resulting quantum graphs are quantum isomorphic to the classical
    derived graphs depicted in the respective figures.
\end{proof}

% TODO: Look at those two paragraphs
\begin{remark}
    \label{rem:scope-representative-graphs}
    We emphasize that this result applies exclusively to the
    representative graphs constructed in this section. Specifically,
    it concerns only those graphs whose Gromada subspace is spanned
    by sums of the blocks \( \mathcal{P}_1,\ldots,\mathcal{P}_4 \)
    introduced in
    Section~\ref{sec:classification-self-adjoint-subspaces}. It does
    not extend to the full family of loop-free, undirected quantum
    graphs on \( (M_{3}(\mathbb{C}), 3\operatorname{Tr}) \), which is
    considerably larger. This distinction becomes immediately
    apparent when comparing our representatives with the complete
    classification of vertex-transitive quantum graphs on \(
    M_{3}(\mathbb{C}) \) by \textcite{hayes26}, which we discuss in
    Section~\ref{sec:vertex-transitive-graphs}.
\end{remark}

\begin{remark}
    \label{rem:quantum-iso-classes}
    Although Theorem~\ref{thm:4matrices} establishes that our
    four representative graphs, as well as the empty graph
    representing \( m = 0 \),
    belong to five \emph{distinct} classes under unitary conjugation,
    this does not guarantee they remain distinct under the strictly
    weaker relation of quantum isomorphism. Because the number of
    quantum edges, \( m \), is not generally a quantum isomorphism
    invariant, representatives with different values of \( m \) could
    theoretically be quantum isomorphic. We do not investigate this
    question here, and we make no claim that the five classical
    graphs exhibited in this section represent distinct quantum
    isomorphism classes.
\end{remark}

\section{Vertex-transitive graphs}
\label{sec:vertex-transitive-graphs}
Classically, a graph is called vertex-transitive if, for any two
vertices \( v, w \), there exists a graph automorphism \( f \) such that
\( f(v) = w \). This notion was later extended to the quantum
setting, and in their recent paper \textcite{hayes26} classify all
vertex-transitive quantum graphs on \( M_{3}(\mathbb{C}) \) up to isomorphism.

\begin{definition}[Vertex-transitive graphs, \textcite{hayes26}, Definition 3.3]
    \label{def:vertex-transitive}
    A quantum graph \( A \) is called \emph{vertex-transitive}
    if the set
    \[
        \{ u \in U(n) : uVu^* = V \}
    \]
    spans \( M_n(\mathbb{C}) \), where \( V \subseteq M_n(\mathbb{C})
    \) is the Gromada subspace of \( A \).
\end{definition}

The representative graphs obtained in Proposition~\ref{thm:4matrices}
are all vertex-transitive, which is proven in the next
Proposition~\ref{prop:self-adjoint-vertex-transitive}. The
proposition below establishes vertex-transitivity for the whole family
of sixteen subspaces generated by \( P \) and \( Q \), not merely the
five representatives.

\begin{proposition}
    \label{prop:self-adjoint-vertex-transitive}
    Every self-adjoint subspace \( V \subseteq M_{3}(\mathbb{C})
    \) spanned by subsets of \( \left\{ P^a Q^b \right\}_{a, b \in
    \left\{ 0, 1, 2 \right\}} \) gives rise to a vertex-transitive
    quantum graph.
\end{proposition}

\begin{proof}
    We first show that conjugation by any element \( P^aQ^b \),
    \( a,b \in \{0,1,2\} \), preserves each one-dimensional
    \( \operatorname{span}\{P^cQ^d\} \) individually. Using the
    commutation relation \( PQ = \omega\,QP \), one computes
    \[
        Q^bPQ^{-b} = \omega^{-b}P, \qquad P^aQP^{-a} = \omega^{a}Q,
    \]
    and more generally, for any generator \( P^cQ^d \),
    \[
        (P^aQ^b)\,P^cQ^d\,(P^aQ^b)^{-1}
        = \omega^{\,bc-ad}\; P^cQ^d.
    \]
    Thus conjugation by \( P^aQ^b \) fixes the generator \( P^cQ^d \)
    up to a scalar phase \( \omega^{bc-ad} \), and in particular sends
    \( \operatorname{span}\{P^cQ^d\} \) to itself.

    Since every block \( \mathcal{P}_k \) is a set of two such
    generators, and every self-adjoint subspace \( V \) is a direct
    sum of some subset of the blocks, it follows that
    \[
        (P^aQ^b)\, V\, (P^aQ^b)^{-1} = V
    \]
    for \emph{every} \( a,b \in \{0,1,2\} \), i.e.\ the full
    group \( \{P^aQ^b\}_{a,b=0,1,2} \) stabilizes
    \( V \), regardless of which subset of blocks was chosen.

    But we have already shown that the nine matrices \( P^aQ^b \) form
    an orthogonal basis of \( M_{3}(\mathbb{C}) \)
    (Lemma~\ref{lemma:pq-orthogonal});
    in particular they
    span \( M_{3}(\mathbb{C}) \). Since
    \[
        \{P^aQ^b\}_{a,b=0,1,2} \;\subseteq\; \{ u \in U(3) : uVu^* = V \},
    \]
    the stabilizer of \( V \) already spans \( M_{3}(\mathbb{C}) \) on
    its own, so \( V \) is vertex-transitive.
\end{proof}

\begin{remark}
    \label{rem:vertex-transitive-all-sixteen}
    In particular, this covers all sixteen subspaces of
    Table~\ref{tab:classification}, including the extremal
    cases \( V_0 = \{0\} \) and \( \mathfrak{su}_3 \), whose
    stabilizers are trivially all of \( U(3) \). More strikingly, the
    proposition shows that vertex-transitivity holds already at the
    \emph{minimal} nontrivial dimension \( m = 2 \): even the smallest
    quantum graphs \( V_k = \operatorname{span}(\mathcal{P}_k) \) are
    vertex-transitive, stabilized by the same \( 9 \)-element group
    that stabilizes every other subspace in the
    classification.
\end{remark}

The following table shows the correspondences between the four
representative quantum graphs of Theorem~\ref{thm:4matrices},
together with the trivial graph, and the vertex-transitive graphs
classified by \textcite[Figure 1]{hayes26}.

\begin{figure}[h]
    \begin{center}
        \begin{tabular}{| c | c | c |}
            \hline
            \( m \) & Quantum graphs from Figure~\ref{fig:four-matrices-m3} &
            Hayes et al. \\
            \hline
            \( m = 0 \) & the empty quantum graph & \(
            \mathrm{VT}^{3}_{0} \) \\
            \hline
            \( m = 2 \) & \( \Gamma_2 \) & \( \mathrm{VT}^{3}_{2} \) \\
            \hline
            \( m = 4 \) & \( \Gamma_4 \) & \( \mathrm{VT}^{3}_{4} \) \\
            \hline
            \( m = 6 \) & \( \Gamma_6 \) & \( \mathrm{VT}^{3}_{6} \) \\
            \hline
            \( m = 8 \) & \( \Gamma_8 \) & \( \mathrm{VT}^{3}_{8} \) \\
            \hline
        \end{tabular}
        \caption{Corresponding quantum graphs in
        \ref{fig:four-matrices-m3} and Hayes et al. }
        \label{tab:our-graphs-are-vertex-transitive}
    \end{center}
\end{figure}
% TODO: fill in the "Ours" column once the correspondence with each
% VT^3_m is checked explicitly.

\begin{remark}
    \label{rem:hayes-classification-is-larger}
    Since \textcite{hayes26} classify \emph{all} vertex-transitive
    quantum graphs on \( M_{3}(\mathbb{C}) \) up to isomorphism, not only those
    arising from subspaces generated by \( P \) and \( Q \), their
    classification is a strict superset of the family constructed in
    Section~\ref{sec:classification-loopfree-undirected-m3}. This
    gives independent confirmation that our five representative graphs
    do not exhaust the loop-free, undirected quantum graphs on
    \( M_{3}(\mathbb{C}) \), which is consistent with the restriction to the
    Pauli-coordinate family already noted in
    Section~\ref{sec:classification-self-adjoint-subspaces}.
    % TODO: once Table~\ref{tab:our-graphs-are-vertex-transitive} is
    % filled in, state explicitly how many additional isomorphism
    % classes appear in Hayes' classification beyond our five.
\end{remark}

\section{General classification of quantum graphs on \( M_3 (\mathbb{C}) \)}
\label{sec:classification-different-types-of-graphs}

In this section, we examine the prospects for classifying quantum
graphs without assuming them to be loop-free or undirected.

Most of the quantum graphs we have been dealing with have been
quantum isomorphic to classical graphs. We will explicitly show a
quantum graph on \( M_{3}(\mathbb{C}) \) that is not isomorphic to
any classical graph.

\begin{example}[\textcite{gromada22}, Example 8.5]
    \label{example:gell-mann-not-classical}
    The matrix
    \[
        \lambda_{8} = \frac{1}{\sqrt{2}}
        \begin{pmatrix}
            1 & 0 & 0 \\
            0 & 1 & 0 \\
            0 & 0 & -2 \\
        \end{pmatrix},
    \]
    which is one of the \emph{Gell--Mann matrices}. Then
    the adjacency matrix \( A = \lambda_{8} \otimes \lambda_{8}
    \) defines a simple graph on \( M_{3}(\mathbb{C}) \). What's more,
    this quantum graph is not quantum isomorphic to any classical graph.
\end{example}

The proof that the quantum graph in
Example~\ref{example:gell-mann-not-classical} is not quantum
isomorphic to any classical graph is non-elementary and is therefore
omitted. We refer the interested reader to \textcite{gromada22} for the details.

Theorem~\ref{thm:2x2-determined-by-m} demonstrates that simple
quantum graphs on \( M_2(\mathbb{C}) \) can be fully classified, establishing
that there are only finitely many such graphs up to isomorphism.

\begin{theorem}[\textcite{gromada22}, Theorem 3.11]
    \label{thm:2x2-determined-by-m}
    A simple quantum graph on \( M_2(\mathbb{C}) \) is determined up to
    isomorphism solely by
    the number of quantum edges \( m \in \{0,1,2,3\} \).
\end{theorem}

The following proof is due to \textcite{gromada22}. We repeat it here
because we wish to point out a step that cannot be replicated in
\( M_3 (\mathbb{C}) \).

\begin{proof}
    \label{prf:1x2-determined-by-m}
    Recall first that \( M_2(\mathbb{C}) \) is assumed to carry
    no loops, which implies that a simple quantum graph on
    \( (M_2(\mathbb{C}), \psi) \) admits at most \( 2^2 - 1 = 3 \)
    quantum edges.

    By Theorem~\ref{thm:gromada}, any simple quantum graph on
    \( M_2(\mathbb{C}) \) with \( m \) quantum edges is uniquely
    determined by an \( m \)-dimensional self-adjoint subspace
    \( V \subseteq \mathfrak{sl}(2) \). By
    Lemma~\ref{lem:self-adjoint-uniqueness}, it suffices to regard
    \( V \) as a real vector space, or equivalently, to identify
    \( \mathrm{i} V \) with a subspace of the Lie algebra
    \( \mathfrak{su}(2) \); by \ref{prop:su2-dimension}, we may then
    naturally view \( V \) as a real
    subspace \( V \subset \mathbb{R}^3 \).

    By Theorem~\ref{thm:gromada}, two such subspaces
    \( V, W \subset \mathbb{R}^3 \) yield isomorphic quantum graphs if
    and only if they are related by conjugation by a unitary matrix
    \( U \in SU(2) \). Under the identification of
    \( \mathfrak{su}(2) \) with \( \mathbb{R}^3 \), this conjugation
    action of \( SU(2) \) corresponds precisely to standard rotations,
    via the canonical isomorphism \( PU(2) \cong SO(3) \).

    Consequently, the quantum graphs corresponding to \( V \) and
    \( W \) are isomorphic if and only if \( V \) can be mapped onto
    \( W \) by a rotation in \( SO(3) \). Since two real subspaces of
    \( \mathbb{R}^3 \) are congruent under \( SO(3) \) if and only if
    they have the same dimension, the isomorphism class of the
    quantum graph is completely determined by \( m = \dim V \).
\end{proof}

The proof of Theorem~\ref{thm:2x2-determined-by-m} relies on the
well-known isomorphism \( PU(2) \cong SO(3) \), which plays a key
role throughout. This result does not generalise to higher
dimensions, and in particular to dimension 3, as the following lemma shows.

\begin{lemma}
    \label{lemma:hard-action}
    The natural action
    \[
        PU(3) \curvearrowright \mathbb{P}(\mathbb{R}^8)
        = \{ W \subseteq \mathbb{R}^8 : \dim(W) = 1 \}
    \]
    has infinitely many orbits.
\end{lemma}

\begin{proof}
    Let \( V = \mathfrak{su}(3) \cong \mathbb{R}^8 \), and identify
    \( \mathbb{P}(\mathbb{R}^8) \) with \( \mathbb{P}(V) \). The action of
    \( PU(3) \) on \( V \) is the adjoint representation
    \[
        g \cdot X = gXg^{-1},
    \]
    which induces an action on the \( 1 \)-dimensional subspaces of
    \( V \). It suffices to exhibit infinitely many pairwise
    non-conjugate lines inside a single explicit family, since this
    already forces infinitely many distinct orbits in \( \mathbb{P}(V) \).

    For \( X \in V \), set
    \[
        q(X) = -\operatorname{Tr}(X^2), \qquad c(X) = i\operatorname{Tr}(X^3).
    \]
    Since the trace is invariant under conjugation,
    \[
        q(gXg^{-1}) = q(X), \qquad c(gXg^{-1}) = c(X),
    \]
    for every \( g \in PU(3) \), and, for every \( \lambda \in \mathbb{R} \),
    \[
        q(\lambda X) = \lambda^2 q(X), \qquad c(\lambda X) = \lambda^3 c(X).
    \]
    Consequently, whenever \( q(X) \neq 0 \), the ratio
    \( c(X)^2/q(X)^3 \) is unchanged both by the adjoint action and by
    rescaling \( X \), and so defines an invariant of the line
    \( \operatorname{span}\{X\} \in \mathbb{P}(V) \) under the induced
    \( PU(3) \)-action.

    Now consider the family
    \[
        X_a = i\operatorname{diag}(a,1,-a-1) \in V, \qquad a \in [0,1].
    \]
    A direct computation gives
    \[
        q(X_a) = 2(a^2+a+1), \qquad c(X_a) = -3a(a+1),
    \]
    and since \( a^2+a+1 = (a+\frac{1}{2})^2 + \frac{3}{4} > 0 \) for
    every \( a \in [0,1] \), we have \( q(X_a) \neq 0 \) throughout
    this range. This lets us define, on the restricted domain \( a
    \in [0,1] \), the
    function
    \[
        I \colon [0,1] \to \mathbb{R}, \qquad
        I(a) = \frac{c(X_a)^2}{q(X_a)^3} = \frac{9a^2(a+1)^2}{8(a^2+a+1)^3},
    \]
    which is exactly the invariant above evaluated on the line
    \( \operatorname{span}\{X_a\} \).

    The function \( I \) is continuous and non-constant on \( [0,1] \);
    for instance,
    \[
        I(0) = 0, \qquad I(1) = \frac{1}{6},
    \]
    so by the intermediate value theorem \( I \) takes infinitely many
    distinct values as \( a \)ranges over \( [0,1] \). Since \( I \) is
    constant on each \( PU(3) \)-orbit meeting the family
    \( \{\operatorname{span}\{X_a\} : a \in [0,1]\} \), distinct values
    of \( I(a) \) force the corresponding lines
    \( \operatorname{span}\{X_a\} \) to lie in distinct orbits. Hence
    this family alone meets infinitely many orbits, so the action of
    \( PU(3) \) on \( \mathbb{P}(\mathbb{R}^8) \) has infinitely many
    orbits.
\end{proof}

As Lemma~\ref{lemma:hard-action} illustrates, the landscape of
quantum graphs on \( M_3(\mathbb{C}) \) is vastly more complex than
that of \( M_2(\mathbb{C}) \). Even in the simplest non-trivial case
of a single quantum edge (\( m=1 \)), there exists an uncountable
family of pairwise non-isomorphic quantum graphs parameterized by
continuous invariants. Consequently, the discrete parameter \( m \)
is entirely insufficient for classification in higher dimensions. A
complete, general classification of quantum graphs on \(
M_3(\mathbb{C}) \) would therefore require navigating a highly
intricate space of \( PU(3) \)-orbits, making the problem
significantly more difficult and fundamentally different from the
finite classification established in Theorem~\ref{thm:2x2-determined-by-m}.
