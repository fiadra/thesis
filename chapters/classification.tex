\chapter{Quantum graphs over \( 3 \times 3 \) matrices}
\label{ch:quantum-graphs-m3}

The classification of quantum graphs on \( 2 \times 2 \) matrices was
carried out independently by \textcite{matsuda22} and \textcite{gromada22}.
In this chapter we attempt a partial classification of quantum graphs on
\( 3 \times 3 \) matrices. This case turns out to be considerably harder
than the \( 2 \times 2 \) case, and a classification of comparable
simplicity is likely out of reach altogether, as we show in
Lemma~\ref{lemma:hard-action}. We conclude the chapter by comparing our
results with those of \textcite{hayes26}.

\section{Pauli matrices and their generalisations}
\label{sec:pauli-matrices-generalisations}

\subsection{Pauli matrices}
\label{subsec:pauli-matrices-m3}

An important basis of the Lie algebra \( \mathfrak{sl}(2,\mathbb{C}) \)
is given by the Pauli matrices
\[
    \sigma_1 =
    \begin{pmatrix} 0 & 1 \\ 1 & 0
    \end{pmatrix}, \qquad
    \sigma_2 =
    \begin{pmatrix} 0 & -i \\ i & 0
    \end{pmatrix}, \qquad
    \sigma_3 =
    \begin{pmatrix} 1 & 0 \\ 0 & -1
    \end{pmatrix}.
\]
The Pauli matrices are Hermitian, traceless, and linearly independent;
since \( \dim_{\mathbb{C}} \mathfrak{sl}(2,\mathbb{C}) = 3 \), they form a
basis of \( \mathfrak{sl}(2,\mathbb{C}) \). They were already encountered
in Example~\ref{example:pauli-matrices}.

Because the Pauli matrices are Hermitian rather than skew-Hermitian, they
do not themselves belong to \( \mathfrak{su}(2) \). Instead, it is the
matrices
\[
    i\sigma_1, \qquad i\sigma_2, \qquad i\sigma_3
\]
that form a basis of \( \mathfrak{su}(2) \), being both traceless and
skew-Hermitian.

\subsection{Generalised Pauli matrices}
\label{subsec:generalised-pauli-matrices}

In dimensions greater than two, the role of the Pauli matrices is played
by the \emph{generalised Pauli matrices}, also known as the \emph{clock}
and \emph{shift} matrices. In dimension three, they are defined by
\[
    P =
    \begin{pmatrix} 1 & 0 & 0 \\ 0 & \omega & 0 \\ 0 & 0 & \omega^2
    \end{pmatrix}, \qquad
    Q =
    \begin{pmatrix} 0 & 0 & 1 \\ 1 & 0 & 0 \\ 0 & 1 & 0
    \end{pmatrix},
\]
where \( \omega = \exp(2\pi i/3) \).

The matrices \( P \) and \( Q \) are unitary and satisfy
\[
    P^3 = Q^3 = I,
\]
together with the commutation relation
\[
    PQ = \omega\, QP.
\]

The clock and shift matrices are related by the discrete Fourier
transform. The Fourier matrix is
\[
    F = \frac{1}{\sqrt{3}}
    \begin{pmatrix} 1 & 1 & 1 \\ 1 & \omega & \omega^2 \\ 1 & \omega^2 & \omega
    \end{pmatrix},
\]
which is unitary, i.e.\ satisfies \( F^*F = FF^* = I \), and exchanges
the roles of the clock and shift operators according to
\[
    FPF^* = Q^{-1}, \qquad FQF^* = P.
\]
Consequently, \( P \) and \( Q \) are unitarily equivalent and represent
two complementary bases of the three-dimensional Hilbert space. This
duality is the finite-dimensional analogue of the relation between
position and momentum operators, and is a fundamental feature of the
generalised Pauli matrices.

\begin{lemma}
    \label{lemma:pq-orthogonal}
    The matrices \( P^aQ^b \), \( a,b \in \{0,1,2\} \), form an
    orthogonal basis of \( M_3 \) with respect to the
    Hilbert--Schmidt inner product \( \langle A,B \rangle =
    \operatorname{Tr}(A^*B) \).
\end{lemma}

\begin{proof}
    There are \( 3^2 = 9 \) matrices of the form \( P^aQ^b \), matching
    \( \dim_{\mathbb{C}} M_3 = 9 \); it therefore suffices to
    show that they are pairwise orthogonal.

    Since \( P^* = P^{-1} \) and \( Q^* = Q^{-1} \), we have
    \( (P^aQ^b)^* = Q^{-b}P^{-a} \), so
    \[
        \langle P^aQ^b, P^cQ^d \rangle
        = \operatorname{Tr}\big(Q^{-b}P^{-a}P^cQ^d\big)
        = \operatorname{Tr}\big(Q^{-b}P^{c-a}Q^d\big).
    \]
    Using the commutation relation \( Q^kP^\ell = \omega^{-k\ell}P^\ell Q^k \),
    we obtain
    \[
        Q^{-b}P^{c-a} = \omega^{b(c-a)}\, P^{c-a}Q^{-b},
    \]
    and hence
    \[
        \langle P^aQ^b, P^cQ^d \rangle
        = \omega^{b(c-a)} \operatorname{Tr}\big(P^{c-a}Q^{d-b}\big).
    \]

    If \( b \neq d \), then \( Q^{d-b} \) is a nontrivial permutation
    matrix, so \( P^{c-a}Q^{d-b} \) has zero diagonal entries and
    \( \operatorname{Tr}(P^{c-a}Q^{d-b}) = 0 \).

    If \( b = d \), then
    \[
        \langle P^aQ^b, P^cQ^b \rangle = \operatorname{Tr}(P^{c-a}).
    \]
    Since \( P^{c-a} = \operatorname{diag}(1,\omega^{c-a},\omega^{2(c-a)}) \),
    \[
        \operatorname{Tr}(P^{c-a}) = 1 + \omega^{c-a} + \omega^{2(c-a)}
        =
        \begin{cases} 3, & a = c, \\ 0, & a \neq c,
        \end{cases}
    \]
    using \( 1 + \omega + \omega^2 = 0 \).

    Combining both cases gives
    \[
        \langle P^aQ^b, P^cQ^d \rangle = 3\,\delta_{ac}\delta_{bd},
    \]
    so the nine matrices \( P^aQ^b \) are pairwise orthogonal, and
    therefore form an orthogonal basis of \( M_3 \).
\end{proof}

Excluding the identity matrix from this basis leaves eight traceless
matrices, which together span \( \mathfrak{sl}_3(\mathbb{C}) \). A basis
of \( \mathfrak{su}_3 \) is then obtained by passing to suitable
skew-Hermitian linear combinations of these eight matrices, exactly as
the passage from \( \sigma_i \) to \( i\sigma_i \) produced a basis of
\( \mathfrak{su}(2) \) above. In this sense, the clock and shift matrices
furnish a natural higher-dimensional generalisation of the Pauli
matrices, suited to describing three-level quantum systems.

% TODO: subsection on Gell--Mann matrices

\section{Classification of self-reflective subspaces spanned by a
    subset of the generalised Pauli basis \( \left\{ P^{a} Q^{b}
\right\}_{a,b \in \mathbb{Z}_3} \)}
\label{sec:classification-self-reflective-subspaces}

By the results of the previous section, it suffices to focus on
self-adjoint subspaces of \( \mathfrak{sl}_3 \). We stress at the
outset, however, that the classification carried out in this section
concerns only \emph{self-reflective subspaces spanned by a subset of
the generalised Pauli basis} \( \{P^aQ^b\} \) -- equivalently, by
Lemma~\ref{lemma:pq-orthogonal}, subspaces that are a direct sum of
some of the reflection-invariant blocks introduced below -- and not
an arbitrary self-adjoint subspace of \( \mathfrak{sl}_3 \). Since
\( \mathfrak{sl}_3 \) is eight-dimensional, an arbitrary subspace need
not be spanned by any subset of a fixed basis: for instance,
\( \operatorname{span}\{P+Q\} \) is a perfectly legitimate
one-dimensional self-adjoint subspace of \( \mathfrak{sl}_3 \) that is
not of this restricted form, and there are in fact continuous families
of subspaces of each dimension that are not coordinate subspaces with
respect to \( \{P^aQ^b\} \). The classification below should therefore
be read as covering a specific, tractable subfamily of the loop-free,
undirected quantum graphs on \( M_3 \), rather than all of them.
% TODO: finish this sentence

\subsection{Fundamental reflection-invariant pairs}
\label{subsec:fundamental-reflection-invariant-pairs}

Since \( P \) and \( Q \) are unitary (\( P^* = P^2 \), \( Q^* = Q^2 \)),
the conjugate transpose of an arbitrary generator satisfies
\[
    (P^aQ^b)^* = \omega^{-ab}\, P^{3-a}Q^{3-b}.
\]
Because any self-reflective subspace spanned by a subset of
\( \{P^aQ^b\} \) must be closed under conjugate transposition, each
basis generator \( P^aQ^b \) must appear alongside
its structural reflection partner \( P^{3-a}Q^{3-b} \). This partitions
the eight non-identity generators into four mutually exclusive,
reflection-invariant blocks of dimension two:
\begin{align*}
    \mathcal{P}_1 &= \{P, P^2\} && \text{(purely diagonal block)} \\
    \mathcal{P}_2 &= \{Q, Q^2\} && \text{(purely shift block)} \\
    \mathcal{P}_3 &= \{PQ, P^2Q^2\} && \text{(first mixed block)} \\
    \mathcal{P}_4 &= \{P^2Q, PQ^2\} && \text{(second mixed block)}
\end{align*}
For instance, the choice \( S = \{1,3\} \) gives the four-dimensional
subspace \( W_{1,3} = \operatorname{span}\{P, P^2, PQ, P^2Q^2\} \).

\subsection{Action of the discrete Fourier transform}
\label{subsec:dft-action-m3}

Let \( F \) denote the \( 3 \times 3 \) discrete Fourier transform
matrix. The unitary similarity transformation \( X \mapsto FXF^* \) acts
as an automorphism of the generalised Pauli algebra, exchanging clock
and shift operators:
\[
    FPF^* = Q^2, \qquad FQF^* = P.
\]
On the fundamental blocks \( \mathcal{P}_k \), this induces a natural
pair-swapping involution:
\[
    F\mathcal{P}_1F^* = \mathcal{P}_2, \qquad
    F\mathcal{P}_2F^* = \mathcal{P}_1, \qquad
    F\mathcal{P}_3F^* = \mathcal{P}_4, \qquad
    F\mathcal{P}_4F^* = \mathcal{P}_3.
\]

\subsection{Subspace classification}
\label{subsec:subspace-classification}

Because the fundamental building blocks \( \mathcal{P}_k \) are
non-overlapping \( 2 \)-dimensional sets, every self-reflective subspace
spanned by a subset of \( \{P^aQ^b\} \) is formed by taking a direct
sum of a subset of these blocks (including the empty set, which yields
the zero subspace).

\begin{proposition}
    \label{prop:subspace-classification-by-dimension}
    Every self-reflective subspace \( V \subseteq M_3 \) generated by
    \( P \) and \( Q \) is classified up to unitary conjugacy by its
    dimension \( m = \dim(V) \),
    as recorded in Table~\ref{tab:classification}.
\end{proposition}

\begin{figure}[h]
    \begin{center}
        \begin{tabular}{| c | c | c |}
            \hline
            \( m \) & subspaces & count \\
            \hline
            \( 1,3,5,7 \) & none exist & \( 0 \) \\
            \hline
            \( 0 \) & \( V_0 = \{0\} \) & \( 1 \) \\
            \hline
            \( 2 \) & \( V_k = \operatorname{span}(\mathcal{P}_k) \),
            \( k \in \{1,2,3,4\} \) & \( 4 \) \\
            \hline
            \( 4 \) & \( W_{i,j} = \operatorname{span}(\mathcal{P}_i
            \oplus \mathcal{P}_j) \), \( 1 \le i < j \le 4 \) & \( 6 \) \\
            \hline
            \( 6 \) & \( U_k = \operatorname{span}\big(\bigoplus_{i
            \neq k} \mathcal{P}_i\big) \), \( k \in \{1,2,3,4\} \) & \( 4 \) \\
            \hline
            \( 8 \) & \( \mathfrak{sl}_3 =
                \operatorname{span}\big(\bigoplus_{k=1}^4
            \mathcal{P}_k\big) \) & \( 1 \) \\
            \hline
        \end{tabular}
        \caption{Classification of self-reflective subspaces
            \( V \subseteq M_3 \) generated by \( P \) and \( Q \), by
        dimension.}
        \label{tab:classification}
    \end{center}
\end{figure}

\begin{corollary}
    \label{cor:sixteen-subspaces}
    Including the trivial subspace \( V_0 \), there exist precisely
    \( 16 \) self-reflective subspaces generated by \( P \) and \( Q \).
\end{corollary}

This classification reduces to elementary combinatorics once the
blocks \( \mathcal{P}_1, \dots, \mathcal{P}_4 \) are fixed. Since these
four blocks partition the eight non-identity Pauli generators into
disjoint reflection-invariant pairs, choosing a self-reflective
subspace \( V \) of this restricted form amounts to nothing more than
choosing a subset
\( S \subseteq \{1,2,3,4\} \) of blocks to include, with
\[
    V = \bigoplus_{k \in S} \mathcal{P}_k.
\]
There are \( 2^4 = 16 \) such subsets in total, matching the count in
the corollary. Moreover, since each included block contributes exactly
two dimensions, the resulting dimension \( \dim(V) \) is always even,
explaining why \( V \) can never have odd dimension. The counts in
each row of the table are then simply the binomial coefficients
\( \binom{4}{r} \), for \( r = |S| \) blocks chosen — namely
\( 1, 4, 6, 4, 1 \) for \( r = 0,1,2,3,4 \) — which is also why the
table is symmetric under \( m \leftrightarrow 8-m \): choosing \( r \)
blocks to include is exactly the same data as choosing \( 4-r \)
blocks to exclude.

\subsection{Full unitary equivalence within each dimension}
\label{subsec:full-unitary-equivalence}

The action of the discrete Fourier transform \( F \) considered above
only produces \( 4 \) orbits among the six four-dimensional subspaces
\( W_{i,j} \), namely \( \{W_{1,2}\}, \{W_{3,4}\},\) \(\{W_{1,3},W_{2,4}\},
\{W_{1,4},W_{2,3}\} \). We now show that this is an artifact of
restricting attention to \( F \) alone: once a second natural unitary
is brought in, \emph{all six} subspaces of dimension \( 4 \) turn out
to be unitarily equivalent -- and, more generally, so are all
subspaces of any fixed dimension \( m \in \{0,2,4,6,8\} \), among those
generated by \( P \) and \( Q \).

Consider the diagonal phase matrix
\[
    S =
    \begin{pmatrix}
        1 & 0 & 0 \\
        0 & 1 & 0 \\
        0 & 0 & \omega \\
    \end{pmatrix}.
\]
Since \( S \) is diagonal, it commutes with \( P \), so
\( SPS^{-1} = P \) and hence \( S \) fixes the block
\( \mathcal{P}_1 = \{P,P^2\} \) point-wise. Its effect on \( Q \) is
more interesting.

\begin{lemma}\label{lem:S-action}
    Conjugation by \( S \) cyclically permutes the blocks
    \( \mathcal{P}_2, \mathcal{P}_3, \mathcal{P}_4 \), while fixing
    \( \mathcal{P}_1 \) point-wise. Explicitly,
    \[
        \begin{aligned}
            SQS^{-1} &= \omega^{-1} PQ, & \qquad SQ^2S^{-1} &= P^2Q^2, \\
            S(PQ)S^{-1} &= \omega^{-1}P^2Q, & \qquad S(P^2Q^2)S^{-1} &= PQ^2.
        \end{aligned}
    \]
\end{lemma}

\begin{proof}
    Write \( e_1,e_2,e_3 \) for the standard basis, so that
    \( Qe_i = e_{i+1} \) (indices mod \( 3 \)) and \( S e_i = s_i e_i \)
    with \( s_1 = s_2 = 1 \), \( s_3 = \omega \). Then
    \[
        (SQS^{-1})e_i = s_i^{-1}(SQ)e_i = s_i^{-1}s_{i+1}\,e_{i+1},
    \]
    and computing the ratios \( s_{i+1}/s_i \) for \( i=1,2,3 \) gives
    \( 1, \omega, \omega^2 \) respectively. Comparing with
    \( (PQ)e_i = p_{i+1}e_{i+1} \), where \( p_1=1,p_2=\omega,p_3=\omega^2 \),
    which gives coefficients \( \omega,\omega^2,1 \), we see that
    \( SQS^{-1} \) has exactly the coefficients of \( PQ \) shifted by
    a global factor of \( \omega^{-1} \); that is,
    \( SQS^{-1} = \omega^{-1}PQ \).

    Squaring, and using the commutation relation
    \( QP = \omega^{-1}PQ \) to simplify
    \( (PQ)^2 = P(QP)Q = \omega^{-1}P^2Q^2 \), we obtain
    \[
        SQ^2S^{-1} = (SQS^{-1})^2 = \omega^{-2}(PQ)^2
        = \omega^{-2}\cdot\omega^{-1}P^2Q^2 = P^2Q^2,
    \]
    since \( \omega^{-3}=1 \).

    For the remaining two identities, use that \( S \) commutes with
    \( P \):
    \[
        S(PQ)S^{-1} = P\,(SQS^{-1}) = P\cdot\omega^{-1}PQ = \omega^{-1}P^2Q,
    \]
    \[
        S(P^2Q^2)S^{-1} = P^2\,(SQ^2S^{-1}) = P^2\cdot P^2Q^2 = P^4Q^2 = PQ^2,
    \]
    using \( P^3 = I \) in the last step. Collecting these four
    identities: \( S \) sends \( \{Q,Q^2\} = \mathcal{P}_2 \) onto
    \( \{PQ, P^2Q^2\} = \mathcal{P}_3 \) (up to phase), and sends
    \( \mathcal{P}_3 \) onto \( \{P^2Q,PQ^2\} = \mathcal{P}_4 \) (up to
    phase). By the same computation applied once more, or simply by
    counting (three blocks permuted among themselves by a single
        conjugation must be permuted cyclically or fixed point-wise, and we
    have already ruled out the latter), \( S \) also sends
    \( \mathcal{P}_4 \) onto \( \mathcal{P}_2 \). This proves the claim.
\end{proof}

In terms of the block indices \( \{1,2,3,4\} \), conjugation by
\( F \) induces the permutation \( \sigma_F = (1\,2)(3\,4) \) (as noted
above), while conjugation by \( S \) induces the \( 3 \)-cycle
\( \sigma_S = (2\,3\,4) \).

\begin{remark}
    \label{rem:a4-no-subgroup-order-six}
    A basic fact from finite group theory will be used below: the
    alternating group \( A_4 \) (of order \( 12 \)) has \emph{no}
    subgroup of order \( 6 \). Indeed, a subgroup of order \( 6 \)
    would have index \( 2 \) in \( A_4 \) and hence be normal, but the
    only normal subgroups of \( A_4 \) are the trivial subgroup, the
    Klein four-group \( V_4 = \{e,(12)(34),(13)(24),(14)(23)\} \), and
    \( A_4 \) itself -- none of order \( 6 \).
\end{remark}

\begin{proposition}\label{prop:full-equivalence}
    The permutations \( \sigma_F \) and \( \sigma_S \) generate the
    full alternating group \( A_4 \) acting on the block indices
    \( \{1,2,3,4\} \). In particular this action is transitive on the
    six unordered pairs \( \{i,j\} \subseteq \{1,2,3,4\} \).
\end{proposition}

\begin{proof}
    Both \( \sigma_F \) (a double transposition) and \( \sigma_S \) (a
    \( 3 \)-cycle) are even permutations, so the group \( H \) they
    generate is a subgroup of \( A_4 \). Since \( H \) contains an
    element of order \( 2 \) and an element of order \( 3 \), its order
    is divisible by \( 6 \). But \( A_4 \) has no subgroup of order
    \( 6 \) by Remark~\ref{rem:a4-no-subgroup-order-six}, and the only
    divisor of \( |A_4|=12 \)
    that is a multiple of \( 6 \) and admits a subgroup is \( 12 \)
    itself; hence \( H = A_4 \).

    It remains to check that \( A_4 \) is transitive on unordered
    pairs. The subgroup of \( A_4 \) fixing a point, say \( 1 \), is
    \( \{e,(2\,3\,4),(2\,4\,3)\} \), which already acts transitively on
    the remaining three points \( \{2,3,4\} \); hence \( A_4 \) is
    \( 2 \)-transitive on \( \{1,2,3,4\} \) (transitive on ordered
    pairs, as \( |A_4| = 12 = 4 \times 3 \)), and in particular
    transitive on unordered pairs.
\end{proof}

\begin{corollary}\label{cor:five-classes}
    For every dimension \( m \in \{2,4,6\} \), any two of the
    subspaces of dimension \( m \) listed in
    Table~\ref{tab:classification} are related by conjugation
    \( V = UWU^* \) for some \( U \in U(3) \), built as a composition
    of the unitary matrices \( F \) and \( S \) (and their inverses). Together
    with the trivial cases \( m=0,8 \), this shows that all sixteen
    self-reflective subspaces generated by \( P \) and \( Q \) fall
    into exactly \emph{five} classes under unitary conjugation, one for
    each dimension \( m \in \{0,2,4,6,8\} \). We emphasize that this
    result concerns only these sixteen subspaces, and that unitary
    conjugation here means \emph{ordinary} isomorphism of quantum
    graphs, in the sense of the first bullet point of
    Definition~\ref{def:quantum-isomorphisms}, not quantum isomorphism.
\end{corollary}

\begin{proof}
    By Proposition~\ref{prop:full-equivalence}, for any two pairs
    \( \{i,j\}, \{i',j'\} \subseteq \{1,2,3,4\} \) there is a
    permutation \( \tau \in A_4 \) with \( \tau(\{i,j\}) = \{i',j'\} \),
    realized as a word in \( \sigma_F, \sigma_S \) and their inverses.
    Replacing each \( \sigma_F \) or \( \sigma_S \) in this word by the
    corresponding unitary \( F \) or \( S \) yields a unitary \( U \)
    with \( U\mathcal{P}_iU^* = \mathcal{P}_{\tau(i)} \) for every block
    index \( i \) (up to scalar phases on individual generators, which
    do not affect the spans), and hence
    \[
        UW_{i,j}U^* = U\big(\operatorname{span}(\mathcal{P}_i \oplus
        \mathcal{P}_j)\big)U^*
        = \operatorname{span}(\mathcal{P}_{\tau(i)} \oplus
        \mathcal{P}_{\tau(j)}) = W_{i',j'}.
    \]
    The same argument, using only the transitivity of \( A_4 \) on
    single points, gives the case \( m=2 \); the case \( m=6 \) follows
    by taking orthogonal complements, since \( U_k \) is precisely the
    orthogonal complement of \( V_k \) inside \( \mathfrak{sl}_3 \), and
    unitary conjugation commutes with taking orthogonal complements.

    Finally, since \( U \) can always be rescaled by a
    scalar \( \lambda \) without changing the conjugation action
    \( (\lambda U)(\cdot)(\lambda U)^* = U(\cdot)U^* \), by choosing
    \( \lambda \in \mathbb{T} \) such that \( \lambda^{3} \det(U) = 1 \), we get
    \( \lambda U \in SU(3) \), matching the form required by Gromada's theorem
    (Theorem~\ref{thm:gromada}) for the corresponding quantum graphs
    to be isomorphic.
\end{proof}

\section{Loop-free and undirected graphs on \( M_3 \) from the
generalised Pauli basis}
\label{sec:classification-loopfree-undirected-m3}

% Corollary 5.3
In this section we exhibit, for each possible number of quantum edges
\( m \in \left\{ 0, 2, 4, 6, 8 \right\} \), a representative quantum
graph among those classified in
Section~\ref{sec:classification-self-reflective-subspaces} -- i.e.\
among the loop-free, undirected quantum graphs on \( M_3 \) whose
Gromada subspace is a direct sum of the blocks
\( \mathcal{P}_1,\ldots,\mathcal{P}_4 \) -- realized as a derived
quantum graph of a voltage quantum graph. As already emphasized in
Section~\ref{sec:classification-self-reflective-subspaces}, this
covers only part of the loop-free, undirected quantum graphs on
\( M_3 \); it is \emph{not} a classification of all such graphs.
Thanks to a result by \textcite{tobolski26}, namely
Theorem~\ref{thm:quantum-iso-different-actions}, those graphs are
quantum isomorphic to quantum
graphs on \( (\mathbb{C}^n \rtimes_{\hat\alpha} \hat{\mathbb{Z}}_n,
\psi_\rtimes) \), which, in turn, is isomorphic to \( (M_3, 3
\operatorname{Tr}) \) by Lemma~\ref{lem:iso}, and so can be thought of as graphs
on \( (M_3, 3\operatorname{Tr}) \). A result by \textcite{tobolski26}
tells us that all those graphs are quantum isomorphic to classical graphs
on 9 vertices.

\begin{proposition}
    \label{prop:4matrices}
    Among the loop-free, undirected quantum graphs on
    \( (M_3, 3\operatorname{Tr}) \) whose Gromada subspace is spanned
    by a subset of the generalised Pauli basis
    \( \{P^aQ^b\}_{a,b\in\mathbb{Z}_3} \) (equivalently, a direct sum
        of some of the blocks
        \( \mathcal{P}_1,\mathcal{P}_2,\mathcal{P}_3,\mathcal{P}_4 \) of
    Section~\ref{sec:classification-self-reflective-subspaces}), there
    is, by Corollary~\ref{cor:five-classes}, exactly one ordinary
    isomorphism class in each nontrivial dimension
    \( m \in \{2,4,6,8\} \). Representative adjacency matrices for
    these four classes, expressed in the basis of generalised and
    normalised Pauli matrices
    (Subsection~\ref{subsec:generalised-pauli-matrices})
    using the formula from Theorem~\ref{thm:gromada}, are as follows:
    \input{matrices}
\end{proposition}

The trivial case \( m=0 \), corresponding to the empty graph, is
omitted, since it carries no meaningful structure to discuss.

The matrices in Proposition~\ref{prop:4matrices} were obtained
by rote computation. For each subspace generated by \( P \) and \( Q \)
as described in
Table~\ref{tab:classification} we calculated the appropriate matrix
using the formula given in Theorem~\ref{thm:gromada} and compared them with
matrices describing derived quantum graphs coming from quantum voltage graphs
\(\tilde{A} = \{A_k\}_{k \in \hat{\mathbb{Z}}_3} \) given by the formula
\[
    \tilde{A} \rtimes_{\hat\alpha} \hat{\mathbb{Z}}_3
    := \sum_{k \in \hat{\mathbb{Z}}_3} m\big( X_k \otimes E_\rtimes^*
    \tilde{A}_k E_\rtimes \big)\, m^*.
\]
The following figures depict the voltage graphs and derived graphs
that are quantum isomorphic to the representative graphs given as
matrices in
Figure~\ref{fig:four-matrices-m3}. Concretely, the voltage graph with
only self-loops (labeled \( 1,2 \) at every vertex) corresponds to the
case \( m=2 \); adding a single \( 1 \)-voltage triangle to these
loops gives \( m=4 \); adding both a \( 1 \)- and a \( 2 \)-voltage
triangle gives \( m=6 \); and the full voltage graph, with edges of
every voltage \( 0,1,2 \) between every pair of vertices, gives
\( m=8 \), the complete quantum graph \( \mathfrak{sl}_3 \).

\input{graphs/ourgraphs}

Since each of these voltage quantum graphs arises, via
Theorem~\ref{thm:quantum-iso-different-actions}, as a trivial-action
derived quantum graph of an ordinary (classical) voltage graph, the
discussion above in fact exhibits an explicit quantum isomorphism
between each of the four representative quantum graphs of
Proposition~\ref{prop:4matrices} and a classical derived graph on the
nine vertices \( \{u,v,w\} \times \hat{\mathbb{Z}}_3 \) shown on the
right of each figure -- concretely realizing the abstract existence
statement from the start of this section.

Two caveats about the scope of this result are worth stating
explicitly. First, this concerns only the representative graphs
constructed in this section -- those whose Gromada subspace is a
direct sum of the blocks \( \mathcal{P}_1,\ldots,\mathcal{P}_4 \) from
Section~\ref{sec:classification-self-reflective-subspaces} -- and not
the full family of loop-free, undirected quantum graphs on
\( (M_3, 3\operatorname{Tr}) \), which is considerably larger, as
already emphasized; a comparison with the complete classification of
vertex-transitive quantum graphs on \( M_3 \) due to
\textcite{hayes26}, given in Section~\ref{sec:vertex-transitive-graphs},
bears this out directly. Second, while Corollary~\ref{cor:five-classes}
shows that our five representatives (including the trivial \( m=0 \)
case) fall into five \emph{distinct} classes under ordinary unitary
conjugation, this does not by itself imply that they remain distinct
under the strictly weaker relation of quantum isomorphism: the number
of quantum edges \( m \) is not in general an invariant of quantum
isomorphism, so two of our representatives with different values of
\( m \) could still, in principle, be quantum isomorphic. We do not
investigate this question here, and make no claim that the five
classical graphs exhibited in this section represent five distinct
quantum-isomorphism classes.

\section{Vertex-transitive graphs}
\label{sec:vertex-transitive-graphs}
Classically, a graph is called vertex-transitive if, for any two
vertices \( v, w \), there exists a graph automorphism \( f \) with
\( f(v) = w \). This notion was later extended to the quantum
setting, and in their recent paper \textcite{hayes26} classify all
vertex-transitive quantum graphs on \( M_3 \) up to isomorphism.

\begin{definition}[Vertex-transitive graphs, \textcite{hayes26}, Definition 3.3]
    \label{def:vertex-transitive}
    A quantum graph \( A \) is called \emph{vertex-transitive}
    if the set
    \[
        \{ u \in U(n) : uVu^* = V \}
    \]
    spans \( M_n \), where \( V \subseteq M_n \) is the subspace associated
    with \( A \).
\end{definition}

The representative graphs obtained in Proposition~\ref{prop:4matrices}
are all vertex-transitive, which is proven in the next
Proposition~\ref{prop:self-reflective-vertex-transitive} -- indeed the
proposition below establishes vertex-transitivity for the whole family
of sixteen subspaces generated by \( P \) and \( Q \), not merely the
five representatives.

\begin{proposition}
    \label{prop:self-reflective-vertex-transitive}
    Every self-reflective subspace \( V \subseteq M_3 \) generated by
    \( P \) and \( Q \) (i.e.\ every direct sum of a subset of the
    blocks \( \mathcal{P}_1, \mathcal{P}_2, \mathcal{P}_3, \mathcal{P}_4 \))
    gives rise to a vertex-transitive quantum graph.
\end{proposition}

\begin{proof}
    We first show that conjugation by any element \( P^aQ^b \),
    \( a,b \in \{0,1,2\} \), preserves each one-dimensional
    \( \operatorname{span}\{P^cQ^d\} \) individually. Using the
    commutation relation \( PQ = \omega\,QP \), one computes
    \[
        Q^bPQ^{-b} = \omega^{-b}P, \qquad P^aQP^{-a} = \omega^{a}Q,
    \]
    and more generally, for any generator \( P^cQ^d \),
    \[
        (P^aQ^b)\,P^cQ^d\,(P^aQ^b)^{-1}
        = \omega^{\,bc-ad}\; P^cQ^d.
    \]
    Thus conjugation by \( P^aQ^b \) fixes the generator \( P^cQ^d \)
    up to a scalar phase \( \omega^{bc-ad} \), and in particular sends
    \( \operatorname{span}\{P^cQ^d\} \) to itself.

    Since every block \( \mathcal{P}_k \) is a set of two such
    generators, and every self-reflective subspace \( V \) is a direct
    sum of some subset of the blocks, it follows that
    \[
        (P^aQ^b)\, V\, (P^aQ^b)^{-1} = V
    \]
    for \emph{every} \( a,b \in \{0,1,2\} \), i.e.\ the full
    group \( \{P^aQ^b\}_{a,b=0,1,2} \) stabilizes
    \( V \), regardless of which subset of blocks was chosen.

    But we have already shown that the nine matrices \( P^aQ^b \) form
    an orthogonal basis of \( M_3 \) (Lemma~\ref{lemma:pq-orthogonal});
    in particular they
    span \( M_3 \). Since
    \[
        \{P^aQ^b\}_{a,b=0,1,2} \;\subseteq\; \{ u \in U(3) : uVu^* = V \},
    \]
    the stabilizer of \( V \) already spans \( M_3 \) on
    its own, so \( V \) is vertex-transitive.
\end{proof}

\begin{remark}
    \label{rem:vertex-transitive-all-sixteen}
    In particular, this covers all sixteen subspaces of
    Table~\ref{tab:classification}, including the extremal
    cases \( V_0 = \{0\} \) and \( \mathfrak{su}_3 \), whose
    stabilizers are trivially all of \( U(3) \). More strikingly, the
    proposition shows that vertex-transitivity holds already at the
    \emph{minimal} nontrivial dimension \( m = 2 \): even the smallest
    quantum graphs \( V_k = \operatorname{span}(\mathcal{P}_k) \) are
    vertex-transitive, stabilized by the same \( 9 \)-element group
    that stabilizes every other subspace in the
    classification.
\end{remark}

The following table shows the correspondences between the (five)
representative quantum graphs of Proposition~\ref{prop:4matrices},
together with the trivial graph, and the vertex-transitive graphs
classified by \textcite[Figure 1]{hayes26}.

\begin{figure}[h]
    \begin{center}
        \begin{tabular}{| c | c | c |}
            \hline
            \( m \) & Our & Theirs \\
            \hline
            \( m = 0 \) & the trivial quantum graph & \(
            \mathrm{VT}^{3}_{0} \) \\
            \hline
            \( m = 2 \) && \( \mathrm{VT}^{3}_{2} \) \\
            \hline
            \( m = 4 \) && \( \mathrm{VT}^{3}_{4} \) \\
            \hline
            \( m = 6 \) && \( \mathrm{VT}^{3}_{6} \) \\
            \hline
            \( m = 8 \) && \( \mathrm{VT}^{3}_{8} \) \\
            \hline
        \end{tabular}
        \caption{Our graphs vs their graphs.}
        \label{tab:our-graphs-are-vertex-transitive}
    \end{center}
\end{figure}
% TODO: fill in the "Ours" column once the correspondence with each
% VT^3_m is checked explicitly.

\begin{remark}
    \label{rem:hayes-classification-is-larger}
    Since \textcite{hayes26} classify \emph{all} vertex-transitive
    quantum graphs on \( M_3 \) up to isomorphism, not only those
    arising from subspaces generated by \( P \) and \( Q \), their
    classification is a strict superset of the family constructed in
    Section~\ref{sec:classification-loopfree-undirected-m3}. This
    gives independent confirmation that our five representative graphs
    do not exhaust the loop-free, undirected quantum graphs on
    \( M_3 \) -- consistent with the restriction to the
    Pauli-coordinate family already noted in
    Section~\ref{sec:classification-self-reflective-subspaces}.
    % TODO: once Table~\ref{tab:our-graphs-are-vertex-transitive} is
    % filled in, state explicitly how many additional isomorphism
    % classes appear in Hayes' classification beyond our five.
\end{remark}

\section{Classification of different types of graphs}
\label{sec:classification-different-types-of-graphs}

In this section, we examine the prospects for classifying quantum
graphs without assuming them to be loop-free or undirected.
Theorem~\ref{thm:2x2-determined-by-m} demonstrates that simple
quantum graphs on \( M_2 \) can be fully classified, establishing
that there are only finitely many such graphs up to isomorphism.

\begin{theorem}[\textcite{gromada22}, Theorem 3.11]
    \label{thm:2x2-determined-by-m}
    A simple graph on \( M_2 \) is determined up to isomorphism solely by
    the number of quantum edges \( m \in \{0,1,2,3\} \).
\end{theorem}

\begin{proof}
    \label{prf:1x2-determined-by-m}
    Recall first that \( V_0 = M_2(\mathbb{C}) \) is assumed to carry
    no loops, which implies that a simple quantum graph on
    \( (M_2(\mathbb{C}), \psi) \) admits at most \( 2^2 - 1 = 3 \)
    quantum edges.

    By Theorem~\ref{thm:gromada}, any simple quantum graph on
    \( M_2(\mathbb{C}) \) with \( m \) quantum edges is uniquely
    determined by an \( m \)-dimensional self-adjoint subspace
    \( V \subset \mathfrak{sl}_2(\mathbb{C}) \). By
    Lemma~\ref{lem:self-adjoint-uniqueness}, it suffices to regard
    \( V \) as a real vector space, or equivalently, to identify
    \( \mathrm{i} V \) with a subspace of the Lie algebra
    \( \mathfrak{su}(2) \); passing to the standard basis given by the
    Pauli matrices, we may then naturally view \( V \) as a real
    subspace \( V \subset \mathbb{R}^3 \).

    By Theorem~\ref{thm:gromada}, two such subspaces
    \( V, W \subset \mathbb{R}^3 \) yield isomorphic quantum graphs if
    and only if they are related by conjugation by a unitary matrix
    \( U \in SU(2) \). Under the identification of
    \( \mathfrak{su}(2) \) with \( \mathbb{R}^3 \), this conjugation
    action of \( SU(2) \) corresponds precisely to standard rotations,
    via the canonical isomorphism \( PU(2) \cong SO(3) \).

    Consequently, the quantum graphs corresponding to \( V \) and
    \( W \) are isomorphic if and only if \( V \) can be mapped onto
    \( W \) by a rotation in \( SO(3) \). Since two real subspaces of
    \( \mathbb{R}^3 \) are congruent under \( SO(3) \) if and only if
    they have the same dimension, the isomorphism class of the
    quantum graph is completely determined by \( m = \dim V \).
\end{proof}

The proof of this theorem relies on the well-known isomorphism
\( PU(2) \cong SO(3) \), which plays a key role throughout. This result
does not generalise to higher dimensions, and in particular to
dimension 3, as the following lemma shows.

\begin{lemma}
    \label{lemma:hard-action}
    The natural action
    \[
        PU(3) \curvearrowright \mathbb{P}(\mathbb{R}^8)
        = \{ W \subseteq \mathbb{R}^8 : \dim(W) = 1 \}
    \]
    has infinitely many orbits.
\end{lemma}

\begin{proof}
    Let \( V = \mathfrak{su}(3) \cong \mathbb{R}^8 \), and identify
    \( \mathbb{P}(\mathbb{R}^8) \) with \( \mathbb{P}(V) \). The action of
    \( PU(3) \) on \( V \) is the adjoint representation
    \[
        g \cdot X = gXg^{-1},
    \]
    which induces an action on the \( 1 \)-dimensional subspaces of
    \( V \). To show that this action has infinitely many orbits, it
    suffices to exhibit a projective invariant taking infinitely many
    values.

    Define
    \[
        q(X) = -\operatorname{Tr}(X^2), \qquad c(X) = i\operatorname{Tr}(X^3).
    \]
    Since the trace is invariant under conjugation,
    \[
        q(gXg^{-1}) = q(X), \qquad c(gXg^{-1}) = c(X)
    \]
    for every \( g \in PU(3) \). Moreover, for every \( \lambda \in
    \mathbb{R} \),
    \[
        q(\lambda X) = \lambda^2 q(X), \qquad c(\lambda X) = \lambda^3 c(X),
    \]
    so the ratio
    \[
        I(X) = \frac{c(X)^2}{q(X)^3}
    \]
    is invariant both under the adjoint action and under rescaling by
    nonzero scalars, and hence defines a well-defined invariant of the
    induced action on \( \mathbb{P}(V) \).

    Consider now the family
    \[
        X_a = i \mathrm{diag}(a, 1, -a-1), \qquad a \in \mathbb{R}.
    \]
    A direct computation gives
    \[
        q(X_a) = 2(a^2+a+1), \qquad c(X_a) = -3a(a+1),
    \]
    and therefore
    \[
        I(X_a) = \frac{9a^2(a+1)^2}{8(a^2+a+1)^3}.
    \]
    This is a non-constant continuous function of \( a \); for instance,
    \[
        I(X_0) = 0, \qquad I(X_1) = \frac{1}{6},
    \]
    so \( I(X_a) \) takes infinitely many values as \( a \) ranges over
    \( \mathbb{R} \). Since \( I \) is constant on each \( PU(3) \)-orbit
    of \( \mathbb{P}(V) \), distinct values of \( I \) must correspond to
    distinct orbits. Hence the action of \( PU(3) \) on
    \( \mathbb{P}(\mathbb{R}^8) \) has infinitely many orbits.
\end{proof}

Thus the argument used in the proof of Theorem~\ref{thm:2x2-determined-by-m}
cannot be used. There is a much wider variety of graphs on \( M_3 \) than on
\( M_2 \).

\begin{proposition}[\textcite{gromada22}, Proposition~3.14]
    \label{prop:inf-many-ex}
    For any \( m = 1, 2, 3 \), there is an infinite family of
    mutually non-isomorphic undirected quantum graphs containing \( m
    \) quantum edges. They can be chosen as follows:
    \begin{itemize}
        \item For \( m = 1 \), we define \( A(t) = P_3(t) \), \( t
            \in [0, \pi/2] \).
        \item For \( m = 2 \), we define \( A(t) = P_3(t) + P_2 \),
            \( t \in [0, \pi/2] \).
        \item For \( m = 3 \), we define \( A(t) = P_3(t) + P_2 + P_1
            \), \( t \in [0, \pi/2] \).
    \end{itemize}
    Here, we denote \( P_i = \sigma_i \otimes \sigma_i^* \), where \(
    \sigma_i \) are the Pauli matrices of
    Example~\ref{example:pauli-matrices}.
    In addition, we define \( P_3(t) = \sigma_3(t)
    \otimes \sigma_3(t)^* \), where \( \sigma_3(t) = \sigma_3 \sin t
    + I_2 \cos t \).

    Every other undirected quantum graph is isomorphic to one of
    these families (or is equal to the empty graph or the full graph).
\end{proposition}

To be more concrete, we can write down explicitly
\[
    P_3(t) =
    \begin{pmatrix}
        1 + \sin(2t) & 0 & 0 & 0 \\
        0 & \cos(2t) & 0 & 0 \\
        0 & 0 & \cos(2t) & 0 \\
        0 & 0 & 0 & 1 - \sin(2t)
    \end{pmatrix}.
\]
For \( t = 0 \), we obtain \( I_2 \), representing the pure
loop. For \( t = \pi/2 \), we obtain \( P_3 \), corresponding to a
loopless edge. For intermediate values \( t \in (0, \pi/2) \), we
obtain a family interpolating between these two configurations.

\begin{proof}
    By Theorem~\ref{thm:gromada}, quantum graphs on \(
    M_2(\mathbb{C}) \) with \( m \) quantum edges stand in one-to-one
    correspondence with \( m \)-dimensional subspaces \( V \subseteq
    \mathfrak{gl}_2(\mathbb{C}) \). Since the quantum graphs are
    assumed to be undirected, Theorem~\ref{thm:gromada} implies that
    \( V \) is self-adjoint (\( V = V^* \)).

    Using the decomposition \( \mathfrak{gl}_2(\mathbb{C}) =
    \mathbb{C} \cdot I_2 \oplus \mathfrak{sl}_2(\mathbb{C}) \), any
    self-adjoint subspace \( V \) admits an orthonormal basis of
    elements of the form
    \[
        v = \lambda I_2 + w, \qquad \lambda \in \mathbb{R}, \quad w
        \in \mathfrak{su}(2).
    \]
    Identifying \( \mathfrak{su}(2) \) with \( \mathbb{R}^3 \) via
    the Pauli basis \( \{\sigma_1, \sigma_2, \sigma_3\} \), each
    matrix \( \sigma_3(t) = \sigma_3 \sin t + I_2 \cos t \)
    corresponds to a vector in \( \mathbb{R}^4 \cong \mathbb{R} \cdot
    I_2 \oplus \mathbb{R}^3 \) with scalar component \( \cos t \) and
    vector component \( \sin t \cdot e_3 \).

    According to Theorem~\ref{thm:gromada}, two subspaces \( V, W \)
    yield isomorphic quantum graphs if and only if \( V = U W U^* \)
    for some unitary \( U \in SU(2) \). Under this adjoint action, \(
    U \) acts trivially on the scalar subspace \( \mathbb{R} \cdot
    I_2 \) and acts by standard rotations via \( PU(2) \cong SO(3)
    \) on the vector space \( \mathbb{R}^3 \).

    Because rotations in \( SO(3) \) preserve the norm of the vector
    component \( |\sin t| \) as well as the scalar component \( \cos
    t \), two elements \( \sigma_3(t_1) \) and \( \sigma_3(t_2) \)
    with \( t_1, t_2 \in [0, \pi/2] \) lie in the same \( SU(2)
    \)-orbit if and only if \( t_1 = t_2 \). Extending this by adding
    the fixed orthogonal direction matrices \( P_2 = \sigma_2 \otimes
    \sigma_2^* \) and \( P_1 = \sigma_1 \otimes \sigma_1^* \), the subspaces
    \[
        V(t) = \operatorname{span}_{\mathbb{C}} \{ \sigma_3(t),
        \sigma_2, \sigma_1 \}_{\text{first } m \text{ elements}}
    \]
    are mutually non-conjugated under \( SU(2) \) for distinct values
    of \( t \in [0, \pi/2] \). Hence, \( A(t) = \sum_{s=1}^m \xi_s(t)
    \otimes \xi_s(t)^* \) defines a continuous, infinite family of
    mutually non-isomorphic quantum graphs for each \( m \in \{1, 2, 3\} \).

    Finally, given any \( m \)-dimensional self-adjoint subspace \( V
    \subseteq \mathfrak{gl}_2(\mathbb{C}) \), we can apply an
    appropriate rotation \( U \in SU(2) \) to align its non-scalar
    projections with the standard Pauli basis vectors. Up to swapping
    generators and scalar shifts, every such subspace is conjugated
    under \( SU(2) \) to precisely one subspace in these
    parameterized families (or to the trivial subspaces corresponding
    to the empty or full graph).
\end{proof}

\subsection{Simple quantum graph that is not quantum isomorphic to a
classical graph}
\label{subsec:simple-graph-not-classical}

Most of the quantum graphs we have been dealing with have been
quantum isomorphic to classical graphs. We will explicitly show a
quantum graph on \( M_3 \) that is not isomorphic to any classical graph.

\begin{example}[\textcite{gromada22}, Example 8.5]
    \label{example:gell-mann-not-classical}
    The matrix
    \[
        \lambda_{8} = \frac{1}{\sqrt{2}}
        \begin{pmatrix}
            1 & 0 & 0 \\
            0 & 1 & 0 \\
            0 & 0 & -2 \\
        \end{pmatrix},
    \]
    which is one of the \emph{Gell--Mann matrices} spanning the
    Lie algebra \( \mathfrak{sl}_3 \). Then
    the adjacency matrix \( A = \lambda_{8} \otimes \lambda_{8}
    \) defines a simple graph on \( M_{3} \).
\end{example}
