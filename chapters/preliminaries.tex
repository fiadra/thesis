\chapter{Preliminaries}

The following sections cover various topics in algebra. They are not
intended to provide an exhaustive treatment; rather, they are meant
to equip the reader with the necessary background to understand the
remainder of this work. References to further information are
provided where appropriate.

\section{Group theory}
\label{sec:group-theory}

In the following section, we will introduce some group theoretic notions that
be used throughout this thesis. We assume that the reader is familiar with
the notion of a group. We denote the torus by \( \mathbb{T} \), i.e. the
set of complex numbers with the absolute value of 1, along with multiplication
as its group action. We denote the identity
element of \( G \) by \( e_{G} \). If \( G \) is abelian, its
identity element is expressed by \( 1_{G} \).

The following definition and proposition will be used throughout the
thesis, as almost all of the groups we will be dealing with
will be finite and abelian.

\begin{definition}[Dual of a group, \textcite{rudin61}, Section 1.2]
    \label{def:dual-group}
    Let \( G \) be a finite abelian group. The \emph{dual group} \(
    \hat{G} \) is the set of all homomorphisms from \( G \) into the torus \(
    \mathbb{T} \) with the group operation defined as
    \[
        \left( \varphi \psi \right)(g) = \varphi(g) \psi(g)
    \]
    for all \( \varphi, \psi \in \hat{G} \) and \( g \in G\), where the latter
    multiplication takes place in \( \mathbb{C} \).
\end{definition}

\begin{remark}
    \label{rem:characters-self-dual}
    We will often call the elements of the dual group
    \emph{characters}. If a group is isomorphic to its dual it will
    be called \emph{self-dual}.
\end{remark}

\begin{proposition}
    \label{prop:finite-abelian-self-dual}
    If \( G \) is finite and abelian, then it is self-dual.
\end{proposition}

\begin{proof}
    First we will prove the statement for finite cyclic groups. Let \( G =
    \left\langle g \right\rangle \), \( \operatorname{ord}(g) = n \). We will
    prove that the dual is cyclic of order \( n \). Let \( \chi\colon
    G \rightarrow \mathbb{T} \) be a character. Since the group is
    cyclic, the character is
    entirely determined by the value it takes on the generator \( g \). Since
    \( g^{n} = e \), obviously \( \chi(g)^{n} = 1 \), so \( \chi(g)
        \in \mu_{n} = \left\{ \exp\left( 2k \pi i / n \right) \colon k =
    0, 1, \dots, n-1 \right\} \subseteq \mathbb{T} \). For each
    possible choice of \(
    \chi(g) \in \mu_{n} \) we get a
    different character. It is then easy to see that \( \chi: G \rightarrow
    \mathbb{T} \) such that \( \chi(g) = \exp\left( 2 \pi i / n \right) \)
    generates this group, i.e. \( \hat{G} = \left\{ \chi, \chi^{2},
    \dots, \chi^{n-1}, \chi^{n} = e_{\hat{G}} \right\} \).

    By the fundamental theorem of finite abelian
    groups (see \textcite[Section 5.2]{dummit04}), since every
    finite abelian group can be written as a finite product
    of finite cycles, all we need to do is check whether the statement
    holds for a product of two groups \( A, B \) that are themselves
    self-dual, i.e. \( \widehat{A \times B} \cong \widehat{A} \times
    \widehat{B} \). This is self-evident as every character
    \( \chi \in \widehat{A \times B} \) is determined by its restrictions
    to \( A \times \{e_{B}\} \) and \( \left\{ e_{A} \right\} \times B \), since
    \[
        \chi(a, b) = \chi((a, e_{B})(e_{A}, b)) = \chi(a, e_{B})\chi(e_{A}, b).
    \]
    Thus \( \chi \leftrightarrow (\chi|_{A}, \chi|_{B})
    \) is an isomorphism.
\end{proof}

\section{\(C^{*}\)-algebras}
\label{sec:cstar-algebras}

In the following section, we will introduce some notions stemming from the
theory of \( C^{*} \)-algebras. We assume the reader is familiar with
the notion of a \(C^{*}\)-algebra. The following definitions are
taken from \textcite{blackadar}.

\begin{definition}[Banach and \(C^{*}\)-algebras, Definition II.1.1.1]
    \label{def:c-star-algebra}
    A \emph{Banach algebra} is a complex algebra which is a Banach
    space under a norm that is submultiplicative, meaning
    \[
        \|ab\| \leq \|a\| \|b\| \quad \text{for all } a, b \in A.
    \]

    An \emph{involution} on a Banach algebra \( A \) is a
    conjugate-linear isometric antiautomorphism of order two, usually
    denoted \( a \mapsto a^* \). In other words, for all \( a, b \in
    A \) and \( \lambda \in \mathbb{C} \), it satisfies:
    \[
        (a + b)^* = a^* + b^*, \quad (ab)^* = b^* a^*, \quad (\lambda
        a)^* = \bar{\lambda} a^*, \quad (a^*)^* = a, \quad \|a^*\| = \|a\|.
    \]

    A \emph{Banach \( * \)-algebra} is a Banach algebra equipped with
    an involution. A Banach algebra \( A \) is called \emph{unital} if there
    is an element \( 1_A \) such that \( 1_A a = a 1_A = a \) for all
    \( a \in A \). Such an element will be called an \emph{unit}.

    An (abstract) \emph{\( C^* \)-algebra} is a Banach \( *
    \)-algebra \( A \) satisfying the \( C^* \)-axiom:
    \[
        \|a^* a\| = \|a\|^2 \quad \text{for all } a \in A.
    \]
\end{definition}

\begin{remark}
    Throughout this thesis we will assume that \(C^{*}\)-algebras are unital.
\end{remark}

Multiplication in a \(C^{*}\)-algebra \( A \) is a bilinear map
\[
    m \colon A \times A \to A.
\]
By the universal property of the tensor product, \( m \) extends uniquely
to a linear map
\[
    \tilde{m} \colon A \otimes A \to A.
\]
We will not distinguish between \( m \) and \( \tilde{m} \) and
simply write \( m \), relying on context to distinguish between the two.

\begin{definition}[Linear functional, II.6.2.1]
    \label{def:linear-functional}
    A \emph{linear functional} on a \(C^{*}\)-algebra \( A \) is a
    linear function \( \psi: A \rightarrow \mathbb{C} \).
\end{definition}

\begin{remark}
    \label{rem:functionals-nickname}
    We will call linear functionals simply as \emph{functionals}.
\end{remark}

The following notions will be useful for defining an inner product on
\(C^{*}\)-algebras. Positiveness and faithfulness are all we need to
define a norm on a finite-dimensional \(C^{*}\)-algebra equipped with
a suitable functional.

\begin{definition}[Positive functional, II.6.7.1]
    \label{def:positive-functional}
    A functional \( \psi \) on a \(C^{*}\)-algebra \( A \) is called
    \emph{positive}, if
    \[
        0\leq \psi(a^{*}a)
    \]
    for any \( a \in A \).
\end{definition}

\begin{definition}[Faithful functional, II.6.7.1]
    \label{def:faithful-functional}
    A positive functional \( \psi \) on a \(C^{*}\)-algebra \( A \) is
    called \emph{faithful}, if
    \[
        \psi(a^{*}a) = 0 \Leftrightarrow a = 0
    \]
    for any \( a \in A \).
\end{definition}

% TODO: What literature?
\begin{remark}
    \label{rem:state-terminology}
    In the literature, a normalised functional \( \psi \) such that
    \( \psi(1) = 1 \) is often called a \emph{state}.
    % For example see \textcite{gromada22}.
\end{remark}

\begin{example}[Positive and faithful functionals on matrix algebras]
    \label{example:trace-functional}
    Let \( A = M_{n}(\mathbb{C}) \), the \(C^{*}\)-algebra of \( n \times n \)
    complex matrices, equipped with the usual trace \( \operatorname{Tr} \).
    Define
    \[
        \psi(a) = n \operatorname{Tr}(a), \qquad a \in A.
    \]
    Since \( a^{*}a \) is positive semi-definite for every \( a \in A \), its
    eigenvalues are non-negative, and hence \(
    \operatorname{Tr}(a^{*}a) \geq 0 \);
    thus \( \psi \) is a positive functional. Moreover, \(
    \operatorname{Tr}(a^{*}a) = 0 \)
    forces all eigenvalues of \( a^{*}a \) to vanish, which (since \( a^{*}a \)
        is positive semi-definite, hence diagonalizable with non-negative
    eigenvalues) implies \( a^{*}a = 0 \) and therefore \( a = 0 \).
    So \( \psi \)
    is faithful.
\end{example}

\begin{example}[A positive functional that is not faithful]
    \label{example:not-faithful-functional}
    Let \( A = \mathbb{C}^{2} = \mathbb{C} \oplus \mathbb{C} \), with
    coordinatewise multiplication and involution given by complex
    conjugation in each coordinate. Define
    \[
        \psi(x,y) = x, \qquad (x,y) \in A.
    \]
    For \( (x,y) \in A \) we have \( (x,y)^{*}(x,y) = (|x|^{2}, |y|^{2}) \),
    so \( \psi\big((x,y)^{*}(x,y)\big) = |x|^{2} \geq 0 \), and \( \psi \) is
    positive. However \( \psi\big((0,y)^{*}(0,y)\big) = 0 \) for every
    \( y \in \mathbb{C} \), while \( (0,y) \neq 0 \) whenever \( y \neq 0 \).
    Hence \( \psi \) is positive but \emph{not} faithful.
\end{example}

The following notion is akin to the notion of a group action on a set.

\begin{definition}[Discrete group action on a \(C^{*}\)-algebra]
    \label{def:group-action-cstar}
    By \emph{group action} of a discrete group \( G \) on a \(C^{*}\)-algebra \(
    A \) we mean a group homomorphism
    \[
        \alpha\colon G \rightarrow \operatorname{Aut}(A).
    \]
    We will write \( g \cdot a \) instead of \( \alpha(g)(a) \) then the action
    in question is clear.
\end{definition}

\begin{example}[Group actions on \(C^{*}\)-algebras]
    \label{example:group-actions-cstar}
    \leavevmode
    \begin{enumerate}
        \item \emph{Trivial action.} For \emph{any} \(C^{*}\)-algebra \( A \)
            and \emph{any} group \( G \), setting \(
            \operatorname{triv}(g)(a) = a \)
            for all \( g \in G,\ a \in A \) trivially defines a group action,
            since \( \operatorname{triv}(g) = \operatorname{id}_A \in
            \operatorname{Aut}(A) \)
            for every \( g \), and the identity map is trivially a group
            homomorphism from \( G \) into \( \operatorname{Aut}(A) \).
        \item \emph{Permutation action.} Let \( A = M_{n}(\mathbb{C}) \)
            and let \( G = S_n \), the symmetric group on \( n \)
            letters. For \( \sigma \in S_n \), let \( P_\sigma \) be
            the associated permutation matrix, i.e.\
            \( P_\sigma e_i = e_{\sigma(i)} \) on the standard basis
            \( \{e_i\} \) of \( \mathbb{C}^n \). Then \( G \) acts on
            \( A \) via
            \[
                \alpha(\sigma)(a) = P_\sigma a P_\sigma^{-1},
                \qquad \sigma \in S_n,\ a \in A,
            \]
            i.e.\ \( \alpha(\sigma) \) simultaneously permutes the
            rows and columns of \( a \) according to \( \sigma \). As
            \( P_\sigma \) is unitary (with \( P_\sigma^{-1} = P_\sigma^* \)),
            each \( \alpha(\sigma) \) is a \( * \)-automorphism of
            \( A \), and since \( \sigma \mapsto P_\sigma \) is itself
            a group homomorphism (\( P_{\sigma\tau} = P_\sigma P_\tau \)),
            we get \( \alpha(\sigma\tau) = \alpha(\sigma)\alpha(\tau) \),
            so \( \alpha \colon S_n \to \operatorname{Aut}(A) \) is
            indeed a group homomorphism.
        \item \emph{Translation action.} Let \( A = C(\mathbb{T}) \), the
            continuous complex-valued functions on the torus \(
            \mathbb{T} \), and let
            \( G = \mathbb{Z} \) act by rotation:
            \[
                (n \cdot f)(z) = f(e^{-in \theta}z), \qquad n \in
                \mathbb{Z},\ f \in A,
            \]
            for a fixed \( \theta \in \mathbb{R} \). This is precisely the
            action used to construct the irrational rotation algebra as a
            crossed product (see
            Example~\ref{example:irrational-rotation-algebra}).
    \end{enumerate}
\end{example}

\subsection{Crossed-product \(C^{*}\)-algebra}
\label{subsec:crossed-product-cstar}

In this subsection, we introduce the notion of a crossed-product
\( C^{*} \)-algebra, in the limited form needed for our purposes; for a
more general treatment we refer the reader to \textcite{blackadar}. The
definitions below follow \textcite{blackadar}.

\begin{definition}[\( C^{*} \)-dynamical system, II.10.3.1]
    \label{def:cstar-dynamical-system}
    A \emph{\( C^{*} \)-dynamical system} is a triple \( (A, G, \alpha) \),
    where \( A \) is a unital \( C^{*} \)-algebra, \( G \) is a discrete group,
    and \( \alpha \colon G \to \operatorname{Aut}(A) \) is a group action
    on \( A \). For a \( C^{*} \)-dynamical system \( (A,G,\alpha) \) we set
    \[
        C_c(G,A) = \{ f \colon G \to A : f \text{ has finite support} \},
    \]
    the space of all \( A \)-valued functions on \( G \) with finite
    support. We usually write \( g \cdot a \) for \( \alpha(g)(a) \).
\end{definition}

If \( G \) is finite, every \( f \colon G \to A \) is automatically
finitely supported. For each \( g \in G \), let
\( u_g \in C_c(G,A) \) denote the function
\[
    u_g(h) =
    \begin{cases}
        1_A, & h = g, \\
        0_A, & h \neq g,
    \end{cases}
    \qquad h \in G,
\]
i.e.\ the indicator function of \( \{g\} \) valued in the unit of
\( A \). Given \( f \in C_c(G,A) \), write \( a_g := f(g) \in A \)
for its values; since \( a_g\, u_g(h) = \delta_{g,h}\,a_g \) (using the
    multiplication of \( A \) and that \( u_g(h) \) is either \( 1_A \) or
\( 0_A \)), \( f \) is recovered uniquely as
\[
    f = \sum_{g \in G} a_g\, u_g,
\]
where the sum is finite since only finitely many \( a_g \) are
nonzero. In this way \( \{u_g\}_{g \in G} \) plays the role of a
formal basis of \( C_c(G,A) \) as an \( A \)-module, with each
\( u_g \) concretely realized as the \( A \)-valued indicator function
of \( g \) above.

The space \( C_c(G,A) \) carries both a product and an
involution, defined precisely so that the group action becomes
\emph{inner}, i.e.\ so that \( g \cdot a = u_g a u_g^* \) for all
\( g \in G \), \( a \in A \). Explicitly, for
\( a = \sum_{g \in G} a_g u_g \) and \( b = \sum_{h \in G} b_h u_h \) in
\( C_c(G,A) \), the product and involution are given by
\[
    ab = \sum_{g,h \in G} a_g\,(g \cdot b_h)\, u_{gh},
    \qquad
    a^* = \sum_{g \in G} (g^{-1} \cdot a_g^*)\, u_{g^{-1}}.
\]

\begin{definition}[\(C^{*}\)-algebra representation, II.6.1.1]
    Let \( A \) be a \(C^{*}\)-algebra. A \emph{\(C^{*}\)-algebra
    representation} \( \pi \)
    is a *-homomorphism \(\pi: A \rightarrow B(\mathcal{H}) \) for some
    Hilbert space
    \( \mathcal{H} \).
    A \(C^{*}\)-algebra representation is called \emph{unital} if
    \( \pi(1_A) = \operatorname{Id}_{\mathcal{H}} \).
\end{definition}

\begin{definition}[Covariant representation, II.10.3.4]
    \label{def:covariant-representation}
    Fix a dynamical system \( (A, G, \alpha) \). A \emph{covariant
    representation} \( (\pi, u, \mathcal{H}) \) of \( (A,G,\alpha) \)
    consists of a unitary representation
    \( u \colon G \to B(\mathcal{H}) \) of \( G \) and a representation
    \( \pi \colon A \to B(\mathcal{H}) \) of \( A \) such that
    \[
        u(g)\,\pi(a)\,u(g)^* = \pi(g \cdot a)
    \]
    for every \( g \in G \) and \( a \in A \).
\end{definition}

\begin{definition}[Full \(C^{*}\)-algebra norm on \(C_c(G,A)\)]
    \label{def:full-cstar-norm}
    The \emph{full \( C^{*} \)-algebra norm} on \( C_c(G,A) \) is
    given by
    \[
        \| \cdot \| = \sup \| (\pi \times u)(\cdot) \|,
    \]
    where the supremum ranges over all covariant representations
    \( (\pi, u, \mathcal{H}) \) of \( (A,G,\alpha) \).
\end{definition}

\begin{definition}[Crossed product \(C^{*}\)-algebra]
    \label{def:crossed-product-cstar-algebra}
    The \emph{crossed product} of a \( C^{*} \)-algebra \( A \) and a
    discrete group \( G \) is the completion of \( C_c(G,A) \)
    with respect to the full norm. We denote it by
    % TODO: notation mismatch -- should this be A \rtimes_\alpha G,
    % matching the A, G, \alpha used to build C_c(G,A,\alpha) above?
    % \tilde{B} \rtimes_{\hat\alpha} \hat{G} introduces B, \hat\alpha,
    % \hat{G} without ever defining them in this subsection.
    \[
        A \rtimes_{\alpha} G.
    \]
\end{definition}

The reader will find some examples of such \(C^{*}\)-algebras below
(Examples~\ref{example:trivial-action-group-algebra}
and~\ref{example:irrational-rotation-algebra}). They are both taken
from \textcite{blackadar}.

\begin{example}[Trivial action recovers the group $C^*$-algebra, II.10.3.15]
    \label{example:trivial-action-group-algebra}
    Let \( A = \mathbb{C} \) with the trivial action \( \alpha_g =
    \operatorname{id} \)
    for all \( g \in G \). Then every element of \( C_c(G,A,\alpha) \) is just
    a finitely supported function \( G \to \mathbb{C} \), with convolution
    product and involution reducing to the usual group algebra structure,
    since the action contributes no twisting. Completing in the full norm
    yields
    \[
        \mathbb{C} \rtimes_\alpha G \;\cong\; C^*(G),
    \]
    the (full) group \( C^* \)-algebra of \( G \). For instance, taking
    \( G = \mathbb{Z} \) gives \( \mathbb{C} \rtimes \mathbb{Z} \cong
    C(\mathbb{T}) \),
    since \( C^*(\mathbb{Z}) \) is generated by a single unitary with no
    relations, i.e.\ the continuous functions on the circle via the
    Fourier transform.
\end{example}

\begin{example}[Irrational rotation algebra, II.10.4.12]
    \label{example:irrational-rotation-algebra}
    Let \( A = C(\mathbb{T}) \), the continuous functions on the torus,
    and let \( G = \mathbb{Z} \) act by rotation through angle
    \( 2\pi\theta \) for some fixed \( \theta \in \mathbb{R}
    \setminus \mathbb{Q} \), i.e.
    \[
        \alpha_n(f)(z) = f\big(e^{-2\pi i n \theta} z\big), \qquad z
        \in \mathbb{T},\; n \in \mathbb{Z}.
    \]
    The resulting crossed product
    \[
        A_\theta \;:=\; C(\mathbb{T}) \rtimes_\alpha \mathbb{Z}
    \]
    is the \emph{irrational rotation algebra} (a non-commutative torus). It
    is generated by a unitary \( u \) (implementing the \( \mathbb{Z} \)-action)
    and a unitary \( v \) (generating \( C(\mathbb{T}) \)) subject to the
    single relation
    \[
        uv = e^{-2\pi i \theta}\, vu,
    \]
    and is a standard first example of a simple, non-commutative
    \( C^* \)-algebra arising from a crossed product.
\end{example}

\section{Matrix Lie Groups and Algebras}
\label{sec:matrix-lie-groups}

In this section we recall some notions from the theory of matrix Lie
groups and algebras that will be needed later in the thesis.

\begin{definition}[Lie group]
    \label{def:lie-group}
    A \emph{Lie group} is a group that is simultaneously a smooth
    manifold, in such a way that both the group multiplication and
    the inversion map are smooth.
\end{definition}

\begin{definition}[Lie algebra]
    \label{def:lie-algebra}
    A \emph{Lie algebra} \( \mathfrak{g} \) is a vector space over a
    field (for us, always \( \mathbb{C} \) or \( \mathbb{R} \))
    equipped with a bilinear form, the \emph{Lie bracket},
    \[
        [\cdot, \cdot] \colon \mathfrak{g} \times \mathfrak{g}
        \rightarrow \mathfrak{g},
    \]
    satisfying:
    \begin{itemize}
        \item \emph{antisymmetry}: \( [x,y] = -[y,x] \);
        \item the \emph{Jacobi identity}:
            \( [x,[y,z]] + [y,[z,x]] + [z,[x,y]] = 0 \).
    \end{itemize}
\end{definition}

\begin{remark}
    \label{rem:lie-group-lie-algebra}
    These two notions are intimately related: every Lie group gives
    rise to a Lie algebra, obtained as the tangent space at the
    identity element, together with a bracket induced by the group
    structure.
\end{remark}

Our primary interest lies in \emph{matrix Lie groups}, i.e.\
subgroups of the general linear group
\[
    GL(n,\mathbb{C}) = \{A \in M_n(\mathbb{C}) : \det(A) \neq 0\},
\]
taken with matrix multiplication as the group operation.

\begin{definition}[Unitary and special unitary groups]
    \label{def:un-sun}
    The \emph{unitary group} is
    \[
        U(n) = \{ U \in M_n(\mathbb{C}) : U^*U = UU^* = I \},
    \]
    and its subgroup, the \emph{special unitary group}, is
    \[
        SU(n) = \{ U \in M_n(\mathbb{C}) : U^*U = UU^* = I,\ \det(U) = 1 \}.
    \]
    Both carry the subspace topology inherited from \( M_n(\mathbb{C}) \).
\end{definition}

\begin{definition}[Projective unitary group]
    \label{def:pun}
    The \emph{projective unitary group} is
    \[
        PU(n) = U(n)/Z(U(n)),
    \]
    where \( Z(U(n)) \) denotes the center of \( U(n) \). Unlike
    \( U(n) \) and \( SU(n) \), the group \( PU(n) \) is defined as a
    \emph{quotient} of \( U(n) \) by a normal subgroup, and is not
    presented here as a subgroup of \( GL(n,\mathbb{C}) \) in its own
    right; it carries the quotient topology induced by the projection
    \( U(n) \to U(n)/Z(U(n)) \), rather than a subspace topology.
\end{definition}

\begin{remark}
    \label{rem:pun-faithful-rep}
    The group \( PU(n) \) does in fact admit a faithful representation
    as a matrix Lie group, e.g.\ via its adjoint action on
    \( \mathfrak{su}(n) \), but we will not need this realization and
    instead work with \( PU(n) \) directly as a quotient group
    throughout.
\end{remark}

\begin{proposition}
    \label{prop:center-un}
    \( Z(U(n)) \cong \mathbb{T} \).
\end{proposition}

\begin{proof}
    Recall that the center of \( U(n) \) is
    \[
        Z(U(n)) = \{ A \in U(n) : AU = UA \text{ for all } U \in U(n) \}.
    \]

    Let \( A \in Z(U(n)) \). We first show that \( A \) is diagonal.
    For any \( \theta \in \mathbb{R} \) and any index \( i \), let
    \( D_i(\theta) \) denote the diagonal unitary matrix with
    \( e^{i\theta} \) in the \( i \)-th diagonal position and \( 1 \)
    elsewhere. Since \( A \) commutes with \( D_i(\theta) \), comparing
    the \( (i,j) \)-entry (for \( j \neq i \)) of both sides of
    \( AD_i(\theta) = D_i(\theta) A \) gives
    \[
        A_{ij}\, 1 = 1\, A_{ij} e^{i\theta}
        \quad\text{and, symmetrically, comparing the } (j,i)\text{-entry,}\quad
        A_{ji}\, e^{i\theta} = A_{ji}.
    \]
    The first identity forces \( A_{ij}(e^{i\theta}-1) = 0 \) for every
    \( \theta \), and since \( e^{i\theta} \neq 1 \) for \( \theta \neq 0 \),
    we get \( A_{ij} = 0 \); the second identity gives \( A_{ji} = 0 \)
    by the same argument. As \( i \) was arbitrary, all off-diagonal
    entries of \( A \) vanish, so \( A \) is diagonal, say
    \( A = \operatorname{diag}(\lambda_1,\dots,\lambda_n) \).

    Next we show that the diagonal entries \( \lambda_1,\dots,\lambda_n \)
    all coincide. Since \( A \) commutes with every unitary matrix, it
    in particular commutes with every permutation matrix. Let
    \( P_{ij} \) denote the permutation matrix exchanging the \( i \)-th
    and \( j \)-th basis vectors, for any \( i \neq j \). Since
    \( P_{ij}AP_{ij}^{-1} \) is the diagonal matrix obtained from \( A \)
    by swapping its \( i \)-th and \( j \)-th diagonal entries, the
    relation \( P_{ij}A = AP_{ij} \), i.e.\
    \( P_{ij}AP_{ij}^{-1} = A \), forces \( \lambda_i = \lambda_j \). As
    this holds for every pair \( i,j \), all diagonal entries of \( A \)
    are equal, so \( A = \lambda I_n \) for some \( \lambda \in \mathbb{C} \),
    where \( I_n \) denotes the \( n \times n \) identity matrix.

    As \( A \in U(n) \), we have \( A^*A = I \), and substituting
    \( A = \lambda I_n \) gives
    \[
        (\overline{\lambda} I_n)(\lambda I_n) = |\lambda|^2 I_n = I_n,
    \]
    so \( |\lambda| = 1 \).

    Conversely, if \( A = \lambda I_n \) with \( |\lambda| = 1 \), then
    clearly \( A \in U(n) \), and for every \( U \in U(n) \),
    \[
        AU = \lambda U = UA,
    \]
    so \( A \in Z(U(n)) \). This proves the claim.
\end{proof}

\begin{definition}[Special orthogonal group]
    \label{def:son}
    The \emph{special orthogonal group} is
    \[
        SO(n) = \{ R \in M_n(\mathbb{R}) : R^TR = RR^T = I,\ \det(R) = 1 \},
    \]
    the Lie group of orientation-preserving rotations fixing the
    origin.
\end{definition}

\begin{proposition}[Matrix commutator]
    \label{prop:matrix-commutator}
    For matrix Lie algebras, the Lie bracket is given by the matrix
    commutator
    \[
        [X,Y] = XY - YX
    \]
    for \( X, Y \) in the Lie algebra; this bracket is antisymmetric
    and satisfies the Jacobi identity.
\end{proposition}

\begin{proof}
    Antisymmetry is immediate: \( [X,Y] = XY-YX = -(YX-XY) = -[Y,X] \).
    For the Jacobi identity, expand each term:
    \begin{align*}
        [X,[Y,Z]] &= X(YZ-ZY) - (YZ-ZY)X = XYZ - XZY - YZX + ZYX, \\
        [Y,[Z,X]] &= YZX - YXZ - ZXY + XZY, \\
        [Z,[X,Y]] &= ZXY - ZYX - XYZ + YXZ.
    \end{align*}
    Summing the three lines, every term cancels against its negative
    counterpart, giving \( 0 \).
\end{proof}

\begin{definition}[\( \mathfrak{gl}_n \) and \( \mathfrak{su}_n \)]
    \label{def:gln-sun-lie-algebras}
    The Lie algebra associated to \( GL(n,\mathbb{C}) \) is denoted
    \( \mathfrak{gl}(n,\mathbb{C}) \) and consists of \emph{all}
    complex \( n \times n \) matrices,
    \[
        \mathfrak{gl}(n,\mathbb{C}) = M_n(\mathbb{C}).
    \]
    The Lie algebra associated to \( SU(n) \), denoted
    \( \mathfrak{su}(n) \), consists of the traceless skew-Hermitian
    matrices,
    \[
        \mathfrak{su}(n) = \{ X \in M_n(\mathbb{C}) : X^* = -X,\
        \operatorname{Tr}(X) = 0 \}.
    \]
\end{definition}

\begin{remark}
    \label{rem:su2-dimension-teaser}
    We may regard \( \mathfrak{su}(2) \) as a \( 3 \)-dimensional real
    vector space (why?); this is used explicitly in
    Example~\ref{example:pauli-matrices} below.
\end{remark}

\begin{example}[The circle group \( SO(2) \) and its Lie algebra]
    \label{example:so2}
    The group \( SO(2) \) consists of rotation matrices
    \[
        R_\theta =
        \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta
        \end{pmatrix},
        \qquad \theta \in [0, 2\pi),
    \]
    and is isomorphic, as a Lie group, to the torus \( \mathbb{T} \) via
    \( e^{i \theta} \mapsto R_\theta \). The corresponding Lie
    algebra \( \mathfrak{so}(2) \) consists of antisymmetric real
    matrices,
    \[
        \mathfrak{so}(2) = \left\{
            \begin{pmatrix} 0 & -t \\ t & 0
        \end{pmatrix} : t \in \mathbb{R} \right\},
    \]
    and is a one-dimensional real vector space. Since any two elements of
    \( \mathfrak{so}(2) \) are scalar multiples of one another, the Lie
    bracket on \( \mathfrak{so}(2) \) vanishes identically: it is an
    \emph{abelian} Lie algebra.
\end{example}

\begin{example}[A basis for \( \mathfrak{su}(2) \)]
    \label{example:pauli-matrices}
    A convenient real basis for \( \mathfrak{su}(2) \) is given by
    \( \{ i\sigma_1, i\sigma_2, i\sigma_3 \} \), where \( \sigma_1,
    \sigma_2, \sigma_3 \) are the
    Pauli matrices
    \[
        \sigma_1 =
        \begin{pmatrix} 0 & 1 \\ 1 & 0
        \end{pmatrix}, \qquad
        \sigma_2 =
        \begin{pmatrix} 0 & -i \\ i & 0
        \end{pmatrix}, \qquad
        \sigma_3 =
        \begin{pmatrix} 1 & 0 \\ 0 & -1
        \end{pmatrix}.
    \]
    Each \( i\sigma_k \) is traceless and skew-Hermitian, so \( i\sigma_k \in
    \mathfrak{su}(2) \), and the three matrices are linearly independent
    over \( \mathbb{R} \); together with \( \dim_{\mathbb{R}} SU(2) = 3 \)
    this confirms that \( \mathfrak{su}(2) \) is a \( 3 \)-dimensional
    real vector space, as anticipated above. The commutation relations
    \[
        [i\sigma_1, i\sigma_2] = -2i\sigma_3, \qquad [i\sigma_2,
        i\sigma_3] = -2i\sigma_1, \qquad
        [i\sigma_3, i\sigma_1] = -2i\sigma_2
    \]
    exhibit \( \mathfrak{su}(2) \) as (a rescaling of) the cross-product
    Lie algebra on \( \mathbb{R}^3 \), and identify \( \mathfrak{su}(2) \)
    with \( \mathfrak{so}(3) \) up to isomorphism.
\end{example}
